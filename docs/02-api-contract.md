# 02 — API Contract（前後端介面契約）

> 擁有者：api-contract-designer。**版本：v0（未凍結）**
> 本文件是 `docs/api/openapi.yaml` 的人讀視圖；兩者衝突時以 yaml 為準。
> 凍結後任何變更需記入 Changelog 並經 architect-reviewer 通過。

## 通用約定
- Base URL：
- 認證方式：
- 統一錯誤格式：
```json
{ "code": "STRING", "message": "STRING", "details": {} }
```
- 分頁慣例：

## Endpoints

### `METHOD /path`
- 說明：
- 認證：需要 / 不需要
- 請求 schema：
- 回應 schema：
- 錯誤碼：
- 範例：
```json
```

## 共用型別（DTO）

## Changelog
| 版本 | 日期 | 變更 | 審查者 |
|---|---|---|---|
