# 審查包（Review Packet）— T-026 編輯活動資訊

- 任務：**T-026** ／ 設計：**D-015**（APPROVED 2026-08-22）／ 風險：**R2**
- 分支：`feat/t-026-edit-event`（未 commit、未 push，由 orchestrator 處理）
- 變更檔案清單（`git diff --stat` + 新增檔）：

```
 src/commands/types.ts                   |  48 ++++++-
 src/commands/parse.ts                   |  77 ++++++++++-
 src/commands/index.ts                   |   4 +-
 src/db/repositories/event-repository.ts |  47 +++++++
 src/domain/event-service.ts             | 238 +++++++++++++++++++++++++++++++-
 src/domain/event-formatter.ts           | 174 ++++++++++++++++++++++-
 src/webhook/handler.ts                  | 112 +++++++++++++++
 src/server.ts                           |   2 +
 （既有測試：僅單測逾時，見 §6-2）
 src/domain/registration-service.add-capacity.test.ts | 4 +
 （新增測試）
 src/commands/__tests__/parse.edit-event.test.ts   | 141 +
 src/domain/event-formatter.edit.test.ts           | 259 +
 src/domain/event-service.edit.test.ts             | 449 +
 src/webhook/handler.edit.test.ts                  | 233 +
 src/__tests__/d015-gates.test.ts                  |  48 +
```

> diff 產法（目錄層級，避免逐檔手誤漏檔）：
> `git diff -- src/` ＋ 上列 5 個 untracked 測試檔全文。（`vitest.config.ts` 已還原為原狀，不在 diff 中。）

## 1. 變更摘要（≤ 5 行）
1. **parser**：`編輯` 分支（`日期／時間／場地（隱藏別名 地點）／費用／人數`）→ `edit_event`／`edit_help`／`invalid(edit_event)`；`InvalidReason` 增 `bad_location`、`invalid` 增選填 `detail:{len}`（供文案顯示實際字數）。
2. **domain**：`EventService.editEvent()` — 交易外只取 `id` 當鎖鍵，`runImmediate(id)`（`FOR UPDATE`）內 `markProcessed` → 鎖內 `getById` 權威重讀 → 狀態/過期 → 授權 → 分派 → 單欄 UPDATE。
3. **repository**：四個單欄寫入原語 `updateEventDatetime`／`updateLocation`／`updatePricePerPerson`／`updateVenueFee`（形狀本身即保證「一次只動一欄」）。
4. **formatter/handler**：§3 釘死文案 + §4 mention（複用既有 `MessageDescriptor.mentionees` → `toLineMessage` → `textV2`+`{mN}`）；`edit_help` 與 `invalid(edit_event)` 都改走 `editEvent()`（否則有回覆卻不 mark）。
5. **未動凍結區**：`src/db/tx.ts` 零修改；`migrations/` 零新增（不新增欄位）。

## 2. Guardrails 自檢表

| Guardrail | 遵守？ | 證據 |
|---|---|---|
| **G1** 決策輸入必於鎖內取得；不繞過/不改 `tx.ts` | ✓ | `event-service.ts` `editEvent()`：交易外 `findActiveByGroup` 的回傳值**只**用 `candidate.id`（見「僅用於取 id 當鎖鍵」註解），其後所有判定與改前值一律取自鎖內 `repos.events.getById(candidate.id)` 的 `fresh`。寫入全部經 `repos.events.*`（client-bound）。`git diff src/db/tx.ts` **為空**。測試：`event-service.edit.test.ts` AC-1（並行不互相覆蓋——若用交易外快照必失敗） |
| **G2** 單次只 UPDATE 一欄；不得寫 capacity/price_mode/status/settled_per_person/group_id/host_user_id | ✓ | 四個原語各只 `SET <單欄>, updated_at`（`event-repository.ts`）；`capacity` 請求走 `return { kind: 'capacity' }`，在任何 UPDATE 之前。測試：AC-2/AC-6/AC-11 的**整列 diff** 斷言（`expectOnlyChanged`）＋ AC-2 的封閉集逐欄斷言 |
| **G3** 不得改到過去 | ✓ | `if (newIso <= now) return { kind: 'past_datetime', now }` 在 `updateEventDatetime` **之前**；`now` 為 service 取一次的同一值。`event_datetime` 的唯一寫入點就是這條分支之後。測試：AC-3（昨日／今日已過時刻／**恰等於 now** 三例＋未來一分鐘反例） |
| **G4** 授權一律走 `authz.canManageEvent`；非授權不 UPDATE、不 upsert users | ✓ | `canManageEventAuthz(repos.users, this.superAdmins, fresh, …)`，未複製任何判定；`UserRepository.upsert` 在 `editEvent` 中**完全未被呼叫**。測試：AC-4（`users` 列數前後相等、`events` 零變動、`processed_events` +1） |
| **G5** 去重全覆蓋，且在所有拒絕 early-return 之前 | ✓ | 兩條路徑（A `runImmediate`／B `tx`）的**第一行**都是 `markProcessed`；handler 的 `invalid{command:'edit_event'}` 不再 `return []` 而送進 `editEvent()`。測試：AC-8 表格驅動涵蓋 11 個分支（含 help 與 format_error 兩條 handler 新路徑）＋ `handler.edit.test.ts` 兩則端到端重送 |
| **G6** 複用既有 validator；**不得**呼叫 `validateFee`；不截斷 | ✓ | `grep -n "validateFee" src/domain/event-service.ts` → 無（只 import `validatePrice`/`validateVenueFee`）；日期/時間走 `validateDate`/`validateTime`（parser 層）；超長一律 `bad_location`，程式中無任何 `slice`/截斷。測試：AC-6、AC-11 |
| **G7** 只用 reply；formatter 不取時鐘；不沿用 `formatFieldError`；示範一律「場地」；球種中性 | ✓ | 無 `push`/`multicast`（沿用既有 reply 管線）；新 formatter 全部以 `now: string` 參數注入（`grep nowIso src/domain/event-formatter.ts` → 無）；新增 `formatEditFormatError` 獨立字串（未呼叫 `formatFieldError`）；AC-10 斷言 help 全文不含「地點」；`event-formatter.edit.test.ts` 末條以禁用詞清單守住球種中性 |
| **G8** mention 走既有機制；只 confirmed、owner 去重、proxy tag 代報者、無 lineUserId 不進 mentionees | ✓ | `tagOwnerIds = [...new Set(listConfirmed(...).map(r => r.owner_user_id))]`（只取 confirmed、天然去重、proxy 列的 owner 即代報者）；輸出仍是 `MessageDescriptor.mentionees` → 既有 `toLineMessage`，未自組 mention JSON。測試：AC-12（domain 層 + handler 層 `textV2`/`{mN}`/不含被代報者名字） |
| **G9** overflow 整則退化；不新增 profile 查詢；`users.getById` N+1 不進鎖 | ✓ | `overflow` 於解析 `line_user_id` **之前**由 `tagOwnerIds.length` 判定；`renderEdit` 在 `overflow` 為真時連 `users.getById` 都不打；逐人解析位於 handler（交易外）。測試：AC-13（上限／上限+1）、AC-14（`getGroupMemberProfile` 呼叫 0 次；鎖內 `users` 查詢 ≤ 1，super-admin 時為 0） |

## 3. Acceptance Checks 對照

| AC | 測試位置（含標記） | 狀態 |
|---|---|---|
| AC-1 併發不互相覆蓋 | `src/domain/event-service.edit.test.ts`「[D-015 AC-1] …真並行」 | ✓ |
| AC-2 RMW 保留另一半 + 逐欄斷言 | 同上「[D-015 AC-2] 改日期保留時間…」＋「[D-015 AC-2] 任一欄編輯皆不得改到 capacity…」 | ✓ |
| AC-3 不得改到過去 | 同上「[D-015 AC-3] 注入固定 now…」；釘死文案在 `event-formatter.edit.test.ts`「[D-015 AC-3] past_datetime…」 | ✓ |
| AC-4 授權 | `event-service.edit.test.ts`「[D-015 AC-4] 非 host 非 admin…」「[D-015 AC-4] host 與 super-admin 皆可成功」；文案在 formatter 測試 | ✓ |
| AC-5 三種拒絕分流 | `event-service.edit.test.ts`「[D-015 AC-5] closed → …」；文案在 formatter 測試；`handler.edit.test.ts`「[D-015 AC-5] 無活動時…」 | ✓ |
| AC-6 費用兩模式且不切換 | `event-service.edit.test.ts` 三條「[D-015 AC-6] …」＋ formatter 測試兩條 | ✓ |
| AC-7 人數導向 | `event-service.edit.test.ts`「[D-015 AC-7] …」、`handler.edit.test.ts`「[D-015 AC-7] …」、formatter 測試 | ✓ |
| AC-8 去重全分支 | `event-service.edit.test.ts`「[D-015 AC-8] G5 列舉之每一分支…」（表格驅動 11 例）＋ `handler.edit.test.ts` 兩條 | ✓ |
| AC-9 parser 契約 | `src/commands/__tests__/parse.edit-event.test.ts`（10 條 `[D-015 AC-9]`）＋ formatter 的格式錯文案 | ✓ |
| AC-10 help 逐字 + 動態日期 + 中性 | `event-formatter.edit.test.ts` 四條 `[D-015 AC-10]`（per_person/split 各逐字比對、兩個 now、不含「地點」、禁用詞清單）＋ `handler.edit.test.ts` split 範例 | ✓（球種中性請 reviewer 覆核，見 §5） |
| AC-11 場地 40 字上限 | `parse.edit-event.test.ts`「[D-015 AC-11]」、`event-service.edit.test.ts`「[D-015 AC-11]」、`handler.edit.test.ts`「[D-015 AC-11]」、formatter 測試 | ✓ |
| AC-12 mention 對象正確 | `event-service.edit.test.ts`「[D-015 AC-12]」（去重/候補/代報）、`handler.edit.test.ts`「[D-015 AC-12]」（`textV2`+`{mN}`）、formatter 測試（無 lineUserId 退化） | ✓ |
| AC-13 超限整則退化 | `handler.edit.test.ts`「[D-015 AC-13] tag 數 = 上限 → 正常；= 上限 + 1 → 退化」＋ formatter 兩條 | ✓（前置已解，見 §5-1） |
| AC-14 不新增 profile 查詢／不延長鎖期 | `handler.edit.test.ts`「[D-015 AC-12]」末段（profile 0 次）、`event-service.edit.test.ts` 兩條「[D-015 AC-14]」 | ✓ |
| AC-15 四關全綠 | `src/__tests__/d015-gates.test.ts`（守住本審查包四關輸出俱全）＋ 下方 §4 實際輸出 | ✓ |

## 3.5 diff 範圍自檢
- [x] §3 AC 對照表點名的每一個檔案，都出現在本包所附的 diff 範圍中
- [x] R2 任務已附全部受影響檔案的 diff，未以「與他任務共用」為由省略任何 hunk
- [x] 產 diff 用目錄層級路徑（`git diff -- src/`）而非逐檔列舉

## 4. 機器關卡結果（lint／build／test 全綠；harness 內容型 check 全綠）

```
$ npm run lint
> eslint .
（無輸出＝0 error / 0 warning）

$ npm run build
> tsc && npm run postbuild
> node scripts/copy-migrations.mjs
[postbuild] 已複製 migrations → …\dist\db\migrations

$ npm test
 Test Files  54 passed (54)
      Tests  430 passed (430)
   Duration  ~95s

$ npm run harness:check      # ← 於寫入 worklist 的 PROPOSE 列**之前**執行
── check_ac_coverage.py
AC 覆蓋：208/208
✓ 全部 AC 皆有對應測試標記
── check_doc_budget.py
✓ 文件預算檢查通過（8 份豁免）
── check_board_sync.py
✓ worklist 與 task-board 同步
✓ harness 關卡全數通過（直譯器：py）

$ npm run harness:check      # ← 交付當下（已寫入 PROPOSE 列）
── check_ac_coverage.py
AC 覆蓋：208/208
✓ 全部 AC 皆有對應測試標記
── check_doc_budget.py
✓ 文件預算檢查通過（8 份豁免）
── check_board_sync.py
同步問題：
  ! backend-engineer.md: T-026 提議 → DONE，但 task-board 尚未裁定
✗ harness 關卡有 1 項未通過
```

**關於 `check_board_sync` 的那一項**：它不是實作缺陷，而是 **PROPOSE → DONE 協定的交接握手**。
`docs/task-board.md` 目前記 T-026 為「派工中」，而 worklist 已依 CLAUDE.md §8／OWNERSHIP 寫下
`PROPOSE → DONE`；該檔**唯一可寫者是 Orchestrator**，實作者不得自行裁定，因此這一項
**只能由 Orchestrator 在裁定時消除**。兩份輸出並列即為證明：內容型的兩個 check
（`check_ac_coverage` 208/208、`check_doc_budget`）在交付前後皆綠，紅的只有這道握手。

基線對照：導入本變更前 `npm test` 為 **371 passed**，本次 **430 passed**（新增 59 條），零回歸。
D-015 的 15 條 AC 已全數納入 `check_ac_coverage`（「待動工豁免」行由 orchestrator 移除後，本包補齊測試標記使之轉綠；**未以任何方式加回豁免、未改 check 腳本、未動 `EXEMPT` 清單**）。

## 5. 需要 reviewer 特別留意的地方

1. **AC-13 前置已解：`MAX_MENTIONS_PER_MESSAGE = 20`，有官方出處。**
   出處為 LINE Messaging API reference → Text message (v2) → **Mention object**：
   「*Up to 20 mentions can be substituted in a single message.*」
   <https://developers.line.biz/en/reference/messaging-api/#text-message-v2-mention-object>
   （機器可讀鏡像：`https://developers.line.biz/en/reference/messaging-api/index.html.md`，該句於 mention object 條列第 5 點。）
   同處另載「mention object 只能用於 reply／push message」「收訊端須為 group／multi-person chat」——本設計走 reply 回群組，皆滿足。
   另已核實 D-015 §4 的敘述屬實：LINE 官方 OpenAPI（`line/line-openapi` 的 `messaging-api.yml`）中 `TextMessageV2.substitution` 為 `additionalProperties` 開放 map、**無 `maxProperties`**，故此上限無法由型別強制，只能在應用層守。

2. **§3 help 模板的 `費用：{費用列}` 重複標籤 —— 已依 orchestrator 裁決修正**（2026-08-23，D-015 errata）。
   詳見 §6-1；本項不再需要 reviewer 裁示，僅需核對前後對照字串。

3. **對 §2 型別表的兩處唯讀補充（無行為變更，但屬設計未涵蓋處）：**
   - `ok` 增 `confirmedCount`：§3 split 成功句需要「目前正取 {K} 人」，K 是正取**列數**，與去重後的 `tagOwnerIds.length` 不同（同一人可多列），無法互相替代。
   - `help` 增 `confirmedCount` 與 `now`：`{費用列}` = `feeLine(event, K, 'live')` 需 K；範例日期需 now（§2 要求 service 取一次後下傳 formatter）。
   另 `ParsedCommand.invalid` 增選填 `detail?: { len: number }`，供 `bad_location` 文案顯示實際字數（AC-11 要求 `{n}` 為實際字數）；此欄 api-contract-designer 更新 `docs/02-api-contract.md` 時需一併納入。

4. **既有 `[D-010 AC-7]` 的逾時假紅 —— 已依 orchestrator 裁決改為單測作用域**（2026-08-23）。
   詳見 §6-2；`vitest.config.ts` 已還原為原狀。

5. **【已採納的刻意偏離，請 reviewer 逐條核對】鎖內 K 的取得方式偏離 N6 字面。**
   - **設計字面（N6）**：`perPersonAmount({...fresh, venue_fee:newFee}, countConfirmed())`。
   - **實作**：`const confirmedRows = await repos.registrations.listConfirmed(fresh.id); const confirmedCount = confirmedRows.length;`（`src/domain/event-service.ts`，見「等價於 countConfirmed()」註解）。
   - **等價論證**：兩個 repo 原語的 WHERE 述詞**逐字相同**——`event_id = $1 AND status = 'confirmed' AND cancelled_at IS NULL`（`registration-repository.ts` 的 `countConfirmed` 與 `listConfirmed`），且兩者若同時呼叫必位於**同一交易、同一快照**（`runImmediate` 的 `FOR UPDATE` 已鎖住該 event，期間無並行者可改動其 registrations），故 `listConfirmed().length ≡ countConfirmed()`。
   - **偏離動機**：`tagOwnerIds` 本來就必須取這批 confirmed 列，改用列數推得可讓鎖內**少一次查詢**，符合 N5「不得延長鎖期」。
   - **N6 的實質要求（以改後值算攤額）仍嚴格遵守**：`perPersonAmount({ ...fresh, venue_fee: r.value }, confirmedCount)` 使用的是**改後** `venue_fee`，非 `fresh.venue_fee`。測試 `[D-015 AC-6]` split 例以 `Math.ceil(4000 / 3)` 斷言（若誤用改前值 3000 會得 1000 而失敗）。
   - **orchestrator 已裁決採納（2026-08-23）**。

## 6. 交付後修正（2026-08-23，依 orchestrator 裁決）

Orchestrator 對 §5 的四點回覆：**兩項要改（下列 6-1／6-2）、兩項照原做法**（`listConfirmed` 推 K → 見 §5-5 已補等價論證；型別三處唯讀補充 → 契約文件由 orchestrator 另行處理，本包未動 `docs/02-api-contract.md`）。

### 6-1 help 費用列去除重複標籤（D-015 §3 errata）

- **設計變更**：§3 help 全文第 5 列由 `費用：{費用列}` 改為 **`{費用列}`**（去掉外層 `費用：`，保留 `feeLine` 自帶標籤）。errata 由 architect 出、orchestrator 已釘死內容。
- **實作**：`src/domain/event-formatter.ts` `formatEditHelp()`
  - 前：`` `費用：${feeLine(event, confirmedCount, 'live')}` ``
  - 後：`feeLine(event, confirmedCount, 'live')`
- **實際輸出前後對照**：

| price_mode | 修正前（重複標籤） | 修正後 |
|---|---|---|
| `per_person` | `費用：每人費用：2500 元` | `每人費用：2500 元` |
| `split_venue` | `費用：場地費：3000 元，平均每人約 750 元（暫估，關閉報名後結算）` | `場地費：3000 元，平均每人約 750 元（暫估，關閉報名後結算）` |

  修正後與名單畫面（`list-formatter` 的 header 費用列）用詞一致。
- **測試**：`src/domain/event-formatter.edit.test.ts` 兩條 `[D-015 AC-10]` 逐字比對期望字串同步更新（per_person／split 各一），並各加一行**回歸守門**——`expect(d.text).not.toContain('費用：每人費用')`／`not.toContain('費用：場地費')`，避免外層標籤日後被改回來。
- 其餘四列（`活動目前資訊：`／`日期：`／`時間：`／`場地：`／`人數上限：`）與四條範例、導向句**皆未變動**。
- **已核對 errata 落地**：`design/D-015-edit-event.md` 的 §3 code block 現為 `{費用列}`（無外層 `費用：`），
  與本實作**逐字一致**；`git diff -- design/D-015-edit-event.md` 為 architect／orchestrator 所改，本包未動該檔。

### 6-2 `[D-010 AC-7]` 逾時改為單測作用域（`vitest.config.ts` 還原）

- **原做法（已撤回）**：`vitest.config.ts` 加全域 `testTimeout: 20_000`。
- **撤回理由（orchestrator）**：全域放寬會讓**任何**真正掛住的測試都要等滿 20 秒才失敗，等於降低整套測試的診斷力；而問題只出在一條測試。
- **現做法**：`vitest.config.ts` **已 `git checkout` 還原為原狀（diff 0 行）**；改在
  `src/domain/registration-service.add-capacity.test.ts` 的該條 `it(...)` 補第三引數 `20_000`，並於其上留三行註解說明成因（999 列 × 約 2000 次 DB 來回，實測 4.5–5.5s，貼近預設 5s 上限）與「刻意不改全域」的理由。
- **影響面**：只有 `[D-010 AC-7]` 一條的逾時閾值改變；其餘 429 條仍受預設 5s 保護。任何斷言未變動。
- 註：此檔屬 D-010 任務的既有測試，本次僅加逾時參數與註解、**未更動任何斷言或測試邏輯**，係依 orchestrator 明示指示執行。

### 6-3 修正後四關複驗

```
$ npm run lint          → 0 error / 0 warning
$ npm run build         → 綠
$ npm test              → Test Files 54 passed (54) ／ Tests 430 passed (430)
$ npm run harness:check → AC 覆蓋 208/208 ✓；文件預算 ✓；board_sync 僅剩 PROPOSE→DONE 握手（見 §4）
```

測試**案數維持 430**（與修正前相同）：6-1 的兩條回歸守門是加在既有 `[D-015 AC-10]` 測試**內**的斷言，
非新增 test case；6-2 只加逾時參數。基線仍為 371 → +59，零回歸。
