# 審查包（Review Packet）— T-018 分組

- 任務：**T-018** ／ 設計：**D-011（APPROVED，含 2026-08-17 errata）** ／ 風險：**R1**
- 審查者：**單一 reviewer（design-reviewer）**——設計已雙審 PASS，本包為實作審查
- 變更檔案清單：

| 檔案 | 增減 | 性質 |
|---|---|---|
| `src/domain/grouping.ts` | +302（新檔） | 純函式：`partitionBalanced` / `scheduleRounds` / `nextRound` |
| `src/domain/grouping-formatter.ts` | +75（新檔） | 純函式中性組版 |
| `src/domain/grouping-service.ts` | +168（新檔） | 名單讀取、host-only 授權、session 讀寫 |
| `src/commands/parse.ts` / `types.ts` | +91 / +20（與 T-019 共用） | `group` / `group_next` 分支 |
| `src/webhook/handler.ts` | （與 T-019/T-020 共用） | `分組`／`下一輪` 接線 |
| `src/server.ts` | +21 | 注入 `GroupingService` |
| 測試 | +197 / +185 / +93 / +53 | `grouping` / `grouping-service` / `grouping-formatter` / `parse.grouping` |

## 1. 變更摘要

新增 `分組`（策略A 均分／策略B `{M}場 [{R}輪] [單打]` 逐輪）與 `下一輪`。名單一律經 `listConfirmed` + `buildRoster` 取得（`grouping-service.ts:101-102`），分組計算為零 DB 副作用純函式；策略B session 以主辦 `line_user_id` 為鍵暫存於既有 `conversation_states`（`state='grouping'`），零 migration。

## 2. Guardrails 自檢表

| Guardrail | 遵守？ | 證據 |
|---|---|---|
| **G1** 不得加欄位/表/migration；session 僅存 `conversation_states`；純函式零 DB | ✓ | `src/db/migrations/` **無新檔**（diff 未觸及）；`grouping-service.ts:133/159` 經 `repos.conversations.upsert` 寫入；`grouping.ts`/`grouping-formatter.ts` 不 import 任何 repository |
| **G2** 文案不得出現特定球種字眼（憲法 §0） | ✓ | `grouping-formatter.test.ts` `[D-011 AC-13]` 直接斷言輸出不含球種字眼 |
| **G3** 只納 confirmed（含 proxy）、排除 waitlist/cancelled，不得另寫查詢 | ✓ | `grouping-service.ts:101` 唯一名單來源 `this.registrations.listConfirmed(eventId)` → `buildRoster`；`[D-011 AC-14]` 以混合資料驗證 |
| **G4** 不得改既有 formatter 措辭或 repository 查詢語意 | ✓ | diff 未包含 `event-formatter.ts`；`list-formatter.ts` 的 +85 行屬 T-019 加開文案，非分組 |
| **G5** 策略/mode 不得落庫於 `events`/`registrations` | ✓ | 無 migration、`event-repository` 僅新增 T-019 的 `updateCapacity`；策略只由當次指令參數決定 |
| **G6** 單打不得產生隊友 pair、不得引入性別邏輯 | ✓ | `[D-011 AC-18]` 斷言單打每場 2 人且無隊友 pair；全 diff 無性別欄位 |

**授權 errata（2026-08-17）**：`分組`/`下一輪` 為 **host-only、排除 super-admin**——`grouping-service.ts:96` `executor.id === event.host_user_id`，**刻意不呼叫 `canManageEvent`**。請 reviewer 確認此處與 T-019 共用 `authz.ts` 的差異是刻意的（是，errata 明訂取代裁決 #4）。

## 3. Acceptance Checks 對照（24/24）

| AC 範圍 | 測試位置 |
|---|---|
| AC-1..12、18、19、21、22（演算法 A/B、邊界、輪空、連續三輪、輪數上限） | `src/domain/grouping.test.ts` |
| AC-5、6、13、15、20（組版格式、中性文案） | `src/domain/grouping-formatter.test.ts` |
| AC-14、23、24（名單組成、無 session、session 落點） | `src/domain/grouping-service.test.ts` |
| AC-16、17（`{M}場{R}輪`、全形、單打解析） | `src/commands/__tests__/parse.grouping.test.ts` |

## 4. 機器關卡結果（家中機器實跑，2026-08-18）

- [x] `npm run lint` 0 ／ `npm run build` 綠
- [x] `npm test`：**343 passed / 343**
- [x] `harness:check --strict`：AC 184/184
- [x] **flaky 複驗**：`grouping*` 三檔連跑 3 次，30/30 全綠、耗時穩定約 1.0s——交接單所憂「AC-7/AC-8 靠 seeded 隨機搜尋（RESTARTS=2000、seed 42）可能 flaky」**未重現**（seed 固定 ⇒ 確定性）

## 5. 需要 reviewer 特別留意的地方

1. **session 與開團問答互斥**：策略B 佔用 `conversation_states` 同一把鍵（主辦 `line_user_id`）。設計已列為已知取捨，但請確認 `handler.ts` 的 conversation 攔截對 `state === 'grouping'` 的分流正確——尤其分組 session 進行中時主辦輸入 `開團` 或流程答案會走哪條路。
2. **`分組` 的 host-only 與其他管理指令不一致**：`關閉報名`/`取消活動`/`加開` 皆為 host ∪ super-admin，唯獨分組排除 super-admin。這是 errata 明訂的，但屬使用者可感知的行為不對稱，請確認拒絕文案（`formatGroupNotHost`）措辭清楚。
3. **N=5 的「一組 5 人」特例**：`[D-011 AC-6]` 產一組 5 人並附註。請確認附註措辭中性且與既有 formatter 風格一致。
