# 審查包（Review Packet）— D-020 切檔（T-033 → T-033a~d）

- 任務：**D-020 切檔**（非程式任務；產出全為設計文件）／設計：D-020 umbrella + D-021~D-029／風險：**R2**
- 交付者：orchestrator（本次由 orchestrator 執行切檔，非 architect）
- 變更檔案清單：

| 檔案 | 變更 |
|---|---|
| `design/D-020-multi-event-per-group.md` | 729 → 210 行；降級為 umbrella 索引 |
| `design/D-021`~`D-029`（9 份，新檔） | 共 742 行；設計內容逐字切出 |
| `docs/task-board.md` | T-033 改為「已拆」父列 + T-033a~d 四列；設計文件狀態表補 D-021~D-029 一列 |
| `docs/proposals/D-020-SPLIT-PROPOSAL.md` | 自 `design/` 移出並加註「已被取代」 |

## 1. 變更摘要（≤5 行）

把 D-020 的設計內容**逐字**切為 9 份子文件（皆 ≤120 行），原 T-033 拆為 4 個可獨立上線的任務。
切法由兩個約束決定：**每份子文件恰屬一個任務**、**每條 AC 皆可在其任務內驗證完畢**。
15 條 Guardrails／30 條 AC 編號一律沿用、不重編、不加前綴；umbrella 保留 §0、切檔與相位、
索引/轉址表、Backlog、errata、討論紀錄。**未改動任何已核可的設計決定**。
唯一的新增**設計**內容標記為 `〔切檔新增〕`／`〔切檔複審…〕`——**本次複審的標的就是這些標記行**
（複審前 20 行、依 blocker 修訂後 42 行）。檔級樣板（狀態／風險／來源／AC 覆蓋宣告）與索引表不屬設計內容。

## 2. Guardrails 自檢表（本次的守則是「切檔不得竄改設計」，非 D-020 的 15 條）

| 條目 | 遵守？ | 證據 |
|---|---|---|
| 設計內容逐字保真，零遺失、零改寫 | ✓ | 九份子文件的設計/G/AC 行串接後與 `git show HEAD:design/D-020-…md` 比對：**512 行零遺失、零重複**，`comm -23` 空集合（見 §4） |
| 新增內容可單點審查、不散落 | ✓ | 20 行全數帶 `〔切檔新增〕` 字面標記，`grep -rn "〔切檔新增〕" design/` 可一次列出（附錄 A） |
| G/AC 編號不變、不重編、不加前綴 | ✓ | 子文件內 AC 標題仍為 `- [ ] **[D-020 AC-n]**`；15 G／30 AC 各恰出現一次（§3） |
| 未新增任何 Guardrail 或設計決定 | ✓ | D-027 的第 3 條為**引用繼承** G8（使用者 2026-09-02 裁決可接受），非新條文；其餘 14 條位置未動 |
| 既有文件零改動 | ✓ | 全庫無任何檔案引用具體 `D-020 AC-n`（已 grep）；11 份 errata 的「來源 D-020」措辭因 errata 不拆而持續成立 |
| 每份子文件恰屬一個任務 | ✓ | D-020 §一索引表；`待動工豁免`宣告因此可於該任務結案時逐檔移除，不跨任務殘留 |

## 3. AC / Guardrail 分配對照

union 檢核（腳本）：**Guardrails 15 條、AC 30 條，各恰一次，無遺漏、無重複**。

| 任務 | 子文件（D-020 舊章節） | Guardrails | AC |
|---|---|---|---|
| T-033a R2 | D-021（§1,§2,§5.1）／D-023（§4.3 核心）／D-024（§4.2,§4.3 截斷）／D-026（§5.2） | G1,G8,G10／G2／G5,G6,G-selector-sync／G11,G12 | 1,2,5／6,8,11,12／9,10,24,29,30／7,20,21,23 |
| T-033b R1 | D-022（§5.4） | G9 | 18,19 |
| T-033c R2 | D-025（§4.1）／D-029（§5.3,§5.5） | G3,G14／G4 | 13,14,15,16,28／17,22 |
| T-033d R2 | D-027（§3）／D-028（§3.5） | G7(+引用繼承 G8)／G13 | 3,4／25,26,27 |

## 4. 機器關卡結果

- [x] `check_doc_budget.py --strict` 通過，**零警示**（10 份設計文件皆 ≤120 行內容）
- [x] `check_ac_coverage.py` 通過（249/249；D-020 已無自帶 AC，D-021~D-029 各自宣告待動工豁免）
- [x] `check_board_sync.py` 通過
- [x] `check_commit_trace.sh` 通過
- [x] 逐字保真腳本：`comm -23 期望 實際` = 空集合（零遺失）；`comm -13` = 20 行，全部帶 `〔切檔新增〕`
- 註：無程式碼變更，故 lint/build/test 不適用（現況 488 tests 全綠，未觸碰）

## 5. 需要 reviewer 特別留意的地方（≤3 點）

1. **【最重要，architect-reviewer】中間狀態不變式**（umbrella §一「切檔與相位」第 1~5 點）。
   「T-033a 上線後、T-033d 上線前：DB 已不硬擋、應用層仍擋」這段期間**從未經 R2 審過**。
   請特別檢驗第 3 點：G7 要求的「應用層 + DB 唯一索引」兩層防護，在該期間是否真的沒有空窗
   （我的論證是：DB 層已於 0006 就位，應用層由**更嚴格的**舊 `already_active` 全面拒絕暫時涵蓋）。
   以及第 2 點對 race 後果的界定（無資料損壞、鎖粒度為單一 event 列）是否成立。
2. **【architect-reviewer】早 DROP 的連帶推論**：`DROP ux_events_active_group` 提前到 T-033a，
   故 G8／AC-5 也必須隨 T-033a 走（舊索引名消失後，`confirm()` 窄捕捉若仍比對舊名，同場地+時間
   的 race 會噴未捕捉的 23505）。請確認此連帶沒有遺漏其他掛在舊索引名上的路徑。
3. **【design-reviewer】文案可達性**：T-033a~c 期間，`ambiguous`／`not_found`／`too_many` 四個新
   文案在 PROD 實務上不可達（開團入口仍擋第二場），但 AC-7 的提示語會叫使用者「回覆或標註
   @場地/@時間」。請確認：(a) 這些釘死字串在切檔後**逐字未變**；(b) 該提示語在 T-033a 上線後
   若真被觸發（極窄 race），指示的兩個動作是否都已可用（`@selector` 隨 T-033a 落地＝可用；
   quote 要等 T-033c＝**不可用**）——這是否構成必須處理的文案缺陷，請裁決。

## 附錄 A：本次唯一需要複審的 20 行（`〔切檔新增〕`，逐字）

### 附錄 A-1：umbrella `D-020` 新增的整節（**本次複審的主標的**）

```markdown
### 切檔與相位（**〔切檔新增〕本節是本次唯一的新增設計內容，須經 R2 複審**）

原 T-033 一次動 4 個 §4.5 高風險模組 + 1 個 migration + 1 個破壞性介面變更（`findActiveByGroup` 移除，
G1）擴散到 6 個呼叫端，既不滿足 CLAUDE.md §3.4「小步提交」也無法獨立回滾。改拆為 4 個可獨立上線的
任務；**每份子文件恰屬一個任務**，其 AC 皆可在該任務內驗證完畢，`待動工豁免`宣告於該任務結案時逐檔
移除，不跨任務殘留。任務內容、子文件與 AC 分配見下方索引表。

**為何機制 B（`@selector`）併入 T-033a 而非獨立成任務**：`resolveTargetEvent` 內部呼叫 `matchSelector`，
且移除 `findActiveByGroup` 後 6 個呼叫端必須立刻有 `eventId` 來源 ⇒ §4.3、§5.2 與 §1／§2／§5.1 是同一
個不可再分的原子；AC-6（單場零回歸）與 AC-23（授權作用對象）亦皆以 `@selector` 構造輸入。這是 T-033a
的下界，再切只會製造無法獨立編譯、或 AC 無法在其任務內驗證的假切分。

**排序關鍵決定**：`DROP ux_events_active_group` 放在 **T-033a（早）** 而非 T-033d（晚）。理由：(i) 晚 DROP
會使 T-033a~c 的多場並存 AC（AC-2／18／23…）在測試 DB 上無法構造真實狀態，只能靠 fixture 手動砍索引，
schema 與 PROD 分歧的品質風險更大；(ii) 早 DROP 的殘餘風險有界，見下方不變式。**連帶**：G8 與 AC-5 隨
T-033a 走——`confirm()` 的窄捕捉必須與舊索引名消失在同一批改為比對新索引名，否則「同場地+時間」的
race 會噴出未捕捉的 23505。

**中間狀態不變式（T-033a 上線後、T-033d 上線前：DB 已不硬擋、應用層仍擋）**：

1. §3 未落地 ⇒ 開團入口仍保留「已有 active 就拒絕」，使用者**無法**建立第二場。
2. 唯一可能產生多場的路徑是「兩人自 0 場同時 `確認`」的極窄 race。若真發生：無資料損壞（鎖粒度本就是
   單一 event 列，不影響超賣防護），讀取指令回既有 `ambiguous` 提示，等其中一場過期或關閉即恢復。
3. 查重的兩層防護無空窗：DB 層（`ux_events_active_group_venue_time` + G8 窄捕捉）已於 T-033a 就位；應用
   層則由**更嚴格的**舊 `already_active` 全面拒絕暫時涵蓋，至 T-033d 交棒給 §3 查重。
4. §3 查重與 §3.5 上限**必須同批**上線（T-033d）：多場並行在該批才對使用者開放，兩道守門同時裝上，不
   存在「已開放但無守門」的空窗。
5. T-033a~c 期間，`ambiguous`／`@selector` 等新文案在 PROD 實務上不可達（見 1、2），屬防禦性路徑。
```

### 附錄 A-2：三段就地新增（相位/脈絡/引用繼承）

```markdown
// design/D-026-dispatch-disambiguation-pipeline.md（§一 開頭）
> **〔切檔新增〕相位說明**：本檔描述 dispatch 管線的**最終狀態**。T-033a 落地時機制 A 尚未實作，
> 故步驟 4 的 `rawQuotedEventId`／`quotedEventId` 兩行與 `resolveQuotedEventInGroup` 恆解出
> `undefined`（等同「未引言」，落既有分支，無新行為）；該段的守門 G14 與 AC-28 屬 **T-033c**（D-025）。
> `splitSelector`（D-024）、`resolveTargetEvent`（D-023）與四個 formatter 隨 T-033a 同批落地。

// design/D-028-group-open-event-limit.md（§一 開頭）
> **〔切檔新增〕脈絡交叉引用**：本檔「不以 DB 約束加固上限、接受 race window」的論證，與 D-021
> §1「同群 open 數上限不在 0006 新增任何索引/約束」是同一個決定的兩面；兩份文件必須一起讀，
> 單讀本檔會以為 DB 層有遺漏。

// design/D-027-duplicate-event-guard.md（§二 第 2 條）
- **G8（〔切檔新增〕引用繼承，不重新定義）**：G8「窄捕捉限定新索引名」的完整條文見 D-021，已隨
  T-033a 與 0006 一併落地。本任務新增的應用層查重**不得**取代、放寬或繞過 `confirm()` 對
  `ux_events_active_group_venue_time` 的窄捕捉——G7 所要求的「DB 唯一索引安全網」那一層即由該窄
  捕捉實現，兩者是同一道防護的上下半，不得因應用層已擋就移除下半。
```

### 附錄 A-3：九份子文件 §三 各一行的測試標記提示（同句型，僅編號不同）

```markdown
> **〔切檔新增〕測試標記一律用本檔編號**：`[D-023 AC-6] …`（AC 編號沿用 D-020 原號不變，但 `check_ac_coverage.py` 依**檔名**判定文件編號，寫 `[D-020 AC-6]` 會對不上）。
```

其餘八份同句型，分別為 `[D-021 AC-1]`／`[D-022 AC-18]`／`[D-024 AC-9]`／`[D-025 AC-13]`／`[D-026 AC-7]`／`[D-027 AC-3]`／`[D-028 AC-25]`／`[D-029 AC-17]`。


---

## 6. 複審結果與處置（2026-09-02，orchestrator 裁定）

**architect-reviewer：BLOCK（3 blocker）／design-reviewer：BLOCK（2 blocker）**——**全數採納**。
orchestrator 未採信回報，逐條對程式碼親自核實後才修訂。

| # | Blocker | 核實證據 | 處置 |
|---|---|---|---|
| A-B1 | 早 DROP 後開團側三處在 T-033a 無替代設計，只能取 `[0]` ⇒ 違反 **G1** | `event-service.ts:342/371`（`already_active` 帶 `event: EventRow`）、`:493` 交易內重讀＋`:499` flip | D-021 增〔切檔複審新增〕**開團側機械替換過渡條文**；明訂取 **`actives.at(-1)`**（舊查詢為 `ORDER BY id DESC LIMIT 1`，取 `[0]` 會靜默取到最舊一場）；宣告為 G1 的明示過渡例外，T-033c 必須整段移除 |
| A-B2 | 早 DROP 使既有測試對舊索引名的斷言失效；**D-007 不在 errata 表** | 7 處：`d007-postgres.test.ts:171`、`event-service.strengthen.test.ts:79`、`event-service.test.ts:291`、`migrate.test.ts:69/90/92/99`、`event-repository.billing.test.ts:158`、`groups-backfill.test.ts:46`、`d008-auto-release.test.ts:180` | umbrella 增**連帶測試窮舉表**（全歸 T-033a）＋ errata 補 `design/D-007` 一列（9 rows/11 docs → 10 rows/12 docs） |
| A-B3 | AC-5 逐字要求 `duplicate_event`，該 kind 屬 §3 ⇒ 無法在 T-033a 驗證 | AC-5 原文 | **AC-5 改隸 D-027（T-033c）**；D-021 加註 T-033a 對 G8 的驗收由既有 `[D-004 AC-12]` 涵蓋 |
| D-B1 | 不變式 #2「等其中一場過期即恢復」**事實錯誤**，且與 #5 矛盾 | `event-status.ts:15` `isExpired` 純判斷不寫 status；`event-repository.ts` `findActiveByGroup` 只比 status；唯一 `UPDATE events SET status` 在 `:135`，由 close/cancel/confirm 觸發 | 改寫 #2：過期不降低候選數；恢復路徑為 `關閉報名`／`取消活動`（需 `@selector`，T-033a 即可用）或下次 `確認` 時 flip 最新過期場。改寫 #5：不可達的是 **quote**，不是 `@selector` |
| D-B2 | AC-7 釘死文案叫使用者「回覆」，但引言要到機制 A 才生效 | AC-7 原文；D-026 相位說明 | **不改釘死字串**（改了會製造第二種說法且 T-033b 後須改回）；D-026 相位註記補明「該期間唯一可用的是 `@selector`」 |

**orchestrator 另行發現、兩位 reviewer 皆未提的第 6 項**：D-022 不可分離——§5.4 明文要求 `getListView`
的候選數判斷「必須先跑 §5.2 的消歧義流程」，是 §5.1／§5.2 的接線約束（G9），獨立成任務會使 T-033a
交付當下即違反 G9；且原標 R1 屬未申報的風險降級（`getListView` 位於 `registration-service.ts`，
D-020 風險列已明列為 §4.5 高風險模組）。**處置：併入 T-033a 並升 R2，任務數 4 → 3。**

**採納的 nit**：D-027 G8 引用繼承措辭（索引本身才是安全網、窄捕捉是其錯誤處理端）／「6 個呼叫端」
實測為 12 處（原文屬已核可內容不改，於 umbrella 補正工時依據）／§2 自檢表「20 行」改述為「新增設計內容」。
**未採納**：無。

**複審後複驗**：逐字保真 **512 行零遺失零重複**（新增 42 行全部帶標記）；G 15／AC 30 各恰一次；
`check_doc_budget --strict`／`check_ac_coverage`（249/249）／`check_board_sync`／`check_commit_trace` 全綠。
**兩份文件超過 R1 的 120 行提示線**（umbrella 146、D-021 133）：R2 依規則四不設上限，且 T-033a 已經
雙審確認為不可再分的原子，**該提示不構成再拆的理由**。
