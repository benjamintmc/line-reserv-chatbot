# 審查包（Review Packet）— T-020 多行批次報名

- 任務：**T-020** ／ 設計：**D-012（APPROVED 2026-08-17）** ／ 風險：**R1**
- 審查者：**單一 design-reviewer**（重點：批次回覆文案／釘死字串）
- 變更檔案清單：

| 檔案 | 增減 | 性質 |
|---|---|---|
| `src/webhook/handler.ts` | +356/−? （與 T-018/T-019 共用） | 拆行、批次分派、合併回覆 |
| `src/webhook/handler.batch.test.ts` | +277（新檔） | D-012 AC-1..9 |

## 1. 變更摘要

`handler.ts` 在 conversation 攔截之後，以 `/\r?\n/` 拆 `text`；`lines.length <= 1` 走原單指令路徑（零回歸），`>= 2` 走批次路徑：逐行 `parseCommand`，**僅** `signup`/`cancel` 可執行，依序 await，每行以複合去重鍵 `${messageId}#${lineIndex}` 呼叫既有 service，最後合併為一次 reply。`registration-service` **完全未動**。

## 2. Guardrails 自檢表

| Guardrail | 遵守？ | 證據 |
|---|---|---|
| **G1** 批次只得執行 signup/cancel，其餘型別忽略 | ✓ | `handler.ts:614-617` 註記 + 可執行行過濾；`[D-012 AC-3]` 以混合行驗證 |
| **G2** 去重鍵必須為 `${message.id}#${lineIndex}`，不得多行共用單鍵 | ✓ | `handler.ts:655` `const compositeId = \`${messageId}#${index}\``；`index` 取自 split 陣列索引（含空行）⇒ 重送命中同鍵 |
| **G3** 不得改 `registration-service` 交易/去重位置/FIFO/超賣語意 | ✓ | **`src/domain/registration-service.ts` 的 diff 全部屬 T-019**（`addCapacity`/`promoteWithinLock`），批次功能未觸及該檔 |
| **G4** 一律一次 reply（≤5 則），不得逐行各發 | ✓ | 批次路徑單一 return，合併訊息陣列 |
| **G5** 單行零回歸 | ✓ | `handler.ts:769` `if (lines.length >= 2)` 才進批次；`[D-012 AC-4]` 斷言單行 `+3` 行為與 D-003 一致 |
| **G6** 超上限不得部分執行 | ✓ | `handler.ts:639-640` 先計 `executables.length > MAX_BATCH_LINES`（=20，`:106`）即 return 拒絕字串，未呼叫任何 service、未 markProcessed |

**釘死字串**（design-reviewer 逐字核對用）：
- 報名行摘要：`已報名：{名字、名字…}`，落候補者名字後標 `（候補）`
- 取消行摘要：`已取消：{名字}`
- 超上限拒絕：`一次最多報名 20 筆，請分次輸入。`

## 3. Acceptance Checks 對照（9/9）

AC-1..9 全數位於 `src/webhook/handler.batch.test.ts`（AC-7 兩案：恰 20 行可執行、21 行整則拒絕）。

## 4. 機器關卡結果（家中機器實跑，2026-08-18）

- [x] `npm run lint` 0 ／ `npm run build` 綠
- [x] `npm test`：**343 passed / 343**
- [x] `harness:check --strict`：AC 184/184
- [x] 零回歸：`handler.test.ts` 既有單指令分派案例全綠

## 5. 需要 reviewer 特別留意的地方

1. **單行尾端換行的行為**：`+3\n` 會被 `/\r?\n/` 拆成 `['+3', '']` ⇒ `lines.length === 2` ⇒ **走批次路徑**（第 2 行為空行被忽略，實際只執行 1 行）。結果正確但去重鍵變成 `${id}#0` 而非 `${id}`——與同一內容不帶尾換行時**鍵不同**。交接單列為「設計字面行為」，請確認可接受；實務影響是使用者若先送 `+3` 再送 `+3\n`（同一 message.id 不會發生，不同 message.id 本就各自去重），風險低但值得記錄。
2. **批次內全非成功時的回覆**：批次內若所有行都失敗，顯示**最後一筆原因一次**。請確認此措辭不會誤導（例如前 3 行額滿轉候補、第 4 行無活動時，只顯示最後一筆）。
3. **全 duplicate 回空不 reply**：對齊 D-003 G9。請確認這與「拒絕回覆去重政策不對稱」這項既有技術債（LESSONS ×3）方向一致，不要在此新增第四種去重政策。
