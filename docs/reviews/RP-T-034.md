# 審查包（Review Packet）— T-034 `編輯 費用` 支援切換計費模式

- 任務：**T-034** ／ 設計：**D-019**（APPROVED 2026-09-01）／ 風險：**R2**（雙審：design-reviewer + architect-reviewer）
- 分支：`feat/t-032-edit-billing-mode`（分支名沿用改號前的 T-032，未更名以免動到已 push 的座標）
- 實作 commit：`fe142d8`（backend-engineer）＋ `d9239ff`（機器關卡實跑後的斷言修正，見 §6）
- diff 產法：`git diff 12bd428..HEAD -- src/`（`12bd428` = 分歧點 `main`）

```
 src/db/repositories/event-repository.ts  |  30 ++---
 src/domain/event-service.ts              |  48 +++++---
 src/domain/event-formatter.ts            |  36 +++++-
 src/webhook/handler.ts                   |   2 +-
 src/domain/event-service.edit.test.ts    | 195 +++++++++++++++++++++++++---
 src/domain/event-formatter.edit.test.ts  |  51 ++++++--
 src/__tests__/d016-security-docs.test.ts |  11 ++
```

## 1. 變更摘要（≤ 5 行）
1. **repo**：新原語 `EventRepository.updateBilling(id, {priceMode, pricePerPerson, venueFee})`，**單一 UPDATE** 同時定三欄；舊的 `updatePricePerPerson`／`updateVenueFee` 兩個單欄原語**已整個移除**（非並存）。
2. **domain**：`editEvent()` 的 `case 'fee'` 改呼叫 `validateFee`（同開團一行式），由其回傳的 `mode` 決定新模式；`switched = mode !== fresh.price_mode`，`fresh` 為鎖內重讀值。
3. **formatter**：新增 `feeLabel(mode, amount)`；`editSuccessLine` 於 `feeModeSwitched === true` 時輸出「已更新計費方式：{帶標籤全稱} → {帶標籤全稱}」，否則沿用 D-015 原兩句型。
4. **型別**：`EditEventResult.bad_fee` 移除 `priceMode` 欄位；`EditOk` 新增選填 `feeModeSwitched?: boolean`。`formatEditBadFee()` 已無參數，唯一呼叫端 `handler.ts:604` 同步更新。
5. **未動凍結區**：`src/db/tx.ts` 與 `src/db/migrations/` 在本 diff 中**零變更**（本案不加欄位、不加 migration）。

## 2. Guardrails 自檢表

| Guardrail | 遵守？ | 證據 |
|---|---|---|
| **G1** 決策輸入必於鎖內取得 | ✓ | `event-service.ts` `case 'fee'` 內所有 `fresh.*` 讀取（`fresh.price_mode`、`fresh.venue_fee`、`fresh.price_per_person`）皆來自 `runImmediate` 內 `getById` 的權威重讀，沿用 D-015 既有結構、本案未新增任何交易外讀取。`switched` 與 `oldAmount` 皆在鎖內計算。測試：AC-6 並行案 |
| **G2** 三欄原子寫入、維持 D-005 不變式 | ✓ | `event-repository.ts:194-197` — `updateBilling` 方法本體**只有一條** `UPDATE events SET price_mode=$1, price_per_person=$2, venue_fee=$3, updated_at=$4 WHERE id=$5`（**請 reviewer 逐字複核此點，屬 AC-7 指定的人工 grep 項**）。不變式由呼叫端釘死：`pricePerPerson = mode==='split_venue' ? 0 : amount`、`venueFee = mode==='split_venue' ? amount : null`。測試：AC-7 spy 斷言單次 `編輯 費用` 恰呼叫一次 |
| **G3** 複用 `validateFee`，不得另寫驗證 | ✓ | `event-service.ts:43` `import { validateFee }`；`:739` 為費用路徑唯一驗證呼叫。`grep validatePrice\|validateVenueFee src/domain/event-service.ts` → **無**（兩者只存在於 `validators.ts` 內部，由 `validateFee` 自己分派）。本案未新增任何 regex |
| **G4** `bad_fee` 與現有模式解耦 | ✓ | `if (!r.ok) return { kind: 'bad_fee' }` 為費用路徑唯一 `bad_fee` 產生點，其判定式不含 `fresh.price_mode`。型別層加固：`bad_fee` 已無 `priceMode` 欄位，`formatEditBadFee()` 已無參數 ⇒ **型別上不可能**依模式分岔文案。測試：AC-4（per_person 與 split 兩模式輸入 `abc` 回同一字串） |
| **G5** 其餘欄位不受影響 | ✓ | 費用路徑唯一寫入呼叫是 `updateBilling`，其 SQL 的 SET 子句只含三欄 + `updated_at`。`capacity`／`status`／`settled_per_person`／`event_datetime`／`location`／`group_id`／`host_user_id` 在本 diff 中無任何新寫入點。測試：AC-6 整列 diff 逐欄斷言（`expectOnlyChanged`） |
| **G6** 通知行為零例外 | ✓ | `tagOwnerIds`／`overflow`／`confirmedCount` 三段程式碼在本 diff 中**未被修改**（沿用 D-015 原碼，位於 `switch` 之前，與 field 無關）。無新增 `push`／`multicast`。測試：AC-5 |

## 3. Acceptance Checks 對照

| AC | 落點 | 狀態 |
|---|---|---|
| AC-1 per_person→split 切換 | `event-service.edit.test.ts:569`、`event-formatter.edit.test.ts:205` | ✓ |
| AC-2 split→per_person 切換 | `event-service.edit.test.ts:595`、`event-formatter.edit.test.ts:222` | ✓ |
| AC-3 同模式只改金額、零回歸 | `event-service.edit.test.ts:260, 285` | ✓（斷言於機器關卡實跑後修正，見 §6） |
| AC-4 真 bad_fee 兩模式同文案 | `event-service.edit.test.ts:323, 340`、`event-formatter.edit.test.ts:143` | ✓ |
| AC-5 mention 行為不變 | `event-service.edit.test.ts:622` | ✓ |
| AC-6 其餘欄位／併發不受影響 | `event-service.edit.test.ts:639, 673` | ✓ |
| AC-7 G2 原子寫入名實相符 | `event-service.edit.test.ts:691`（spy）＋ 本包 §2 G2 列的人工 grep | ✓（見 §5-2 的方法論偏離） |
| AC-8 機器關卡全綠 | `src/__tests__/d016-security-docs.test.ts:86` ＋ 本包 §4 輸出 | ✓ |

## 4. 機器關卡結果（2026-09-02，orchestrator 於具備 node+docker 的機器實跑）

| 關卡 | 結果 |
|---|---|
| `npm run lint` | ✓ eslint 0 問題 |
| `npm run build` | ✓ `tsc` + postbuild 通過 |
| `npm test` | ✓ **487 passed / 487**，62 test files（`main` 基線 477） |
| `npm run harness:check` | ✓ AC 覆蓋 **249/249**、commit trace、board sync 全過（D-020 30 條 AC 為顯式列名的待動工豁免，不計入） |

> 註：`docs/task-board.md` 上一版記載「四關未跑」係因前一台機器無 node/npm；本輪為首次實跑。

## 5. 需要 reviewer 特別留意的地方

**5-1（architect-reviewer）`feeLabel` 造成 domain → formatter 的一條 value import。**
`event-service.ts:47` `import { feeLabel } from './event-formatter'`。這是本 repo 中 domain 對 formatter 的**唯一**執行期依賴（其餘方向皆為 `import type`，無執行期循環）。**這是 D-019 §一.3／§一.5 的字面規定**——設計文件明文把 `feeLabel` 定義為「formatter 內部 helper」，backend-engineer 依文件實作、未擅自更動位置。請裁定：接受此偏離（並在 D-019 補 errata 記明），或要求把 `feeLabel` 下移至 domain／共用模組。

**5-2（兩位）AC-7 的證明方式與文件字面不同。**
D-019 AC-7 原文要求斷言「未呼叫 `updatePricePerPerson`／`updateVenueFee`」。實作選擇的是更強的做法：**兩個舊原語已從 `event-repository.ts` 整個移除**（`grep -rn "updatePricePerPerson\|updateVenueFee" src/` 僅命中一行說明註解），型別層面不可能呼叫到，故未再寫 `not.toHaveBeenCalled()`（該方法已不存在，寫不出來）。請裁定此等價性是否成立。

**5-3（design-reviewer）新 `bad_fee` 文案與 D-005 §6.2 的措辭刻意不逐字同款。**
`formatEditBadFee()` 的字串為 D-019 §5 釘死值，設計文件已註明「呼應 D-005 §6.2 精神、非逐字同款」（列點式 vs 問答句屬情境差異的合理分歧）。請確認實作與 D-019 §5 **字元級**一致，而非與 D-005 一致。

**5-4（design-reviewer）球種中性。**
新增文案為「已更新計費方式」「每人費用 N 元」「場地費 N 元」及新 `bad_fee` 句，均為中性用語；請依 CLAUDE.md §0 複核無特定球種用語混入。

## 6. 交付後修正（2026-09-02，orchestrator）

實作階段的機器（前一台）無 node/npm，測試從未被執行過。本輪首次實跑抓到 2 條斷言假設錯誤：

- `event-service.edit.test.ts` 兩處期待同模式時 `feeModeSwitched` 欄位被省略（`toBeUndefined()`），但 D-019 §一.3 明寫 `feeModeSwitched = switched`，未切換即 `false`。已改為 `toBe(false)`。
- **使用者可見行為零變動**：`event-formatter.ts:337` 的判斷式為 `r.feeModeSwitched === true`，`false` 與 `undefined` 走同一分支、輸出同一字串。
- 另補 D-019 AC-8 的落點測試（`d016-security-docs.test.ts`），沿用 D-014 AC-5 既有做法。

commit `d9239ff`。修正後四關全綠（即 §4 的數字）。

## 7. R2 雙審結果（2026-09-02）

### architect-reviewer：**PASS**（零 blocker、4 nit、2 項待補 errata）
G1–G6 逐條通過並附 diff 行號證據。三項專項裁定：
- **§5-1 `feeLabel` value import → 接受，不搬移。** 關鍵事實是 `event-formatter.ts` **就位於 `src/domain/` 底下**，屬**層內**依賴而非跨層，`01-architecture.md` 的分層未被破壞；formatter 對 event-service 僅 `import type`（編譯期抹除），執行期為單向邊、無循環。
- **§5-2 AC-7 等價性 → 成立，且更強。** 原文的 `not.toHaveBeenCalled()` 是執行期、只覆蓋被測路徑；實作把兩個舊原語整個移除 ⇒ 編譯期全域不可能，涵蓋範圍嚴格更大。
- **凍結區 → 複核通過。** 全 diff 僅 6 檔，`src/db/tx.ts` 與 `src/db/migrations/` 零 hunk。

### design-reviewer：**BLOCK → 已封閉**（1 blocker、5 nit）
釘死字串 5 條逐字元比對全數一致（含全形/半形、空格位置、`U+2192` 前後空白）；球種中性 6 句 PASS；切換/未切換的刻意不對稱裁定為成立、不製造困惑；「暫估，關閉報名後結算」為 D-015 既有字串原樣沿用、無第二種說法。

**B1（切換成 split_venue 且正取 K=0 的文案無測試覆蓋）— orchestrator 查證後裁定：**
- **審查員疑慮的 Infinity/NaN 不成立**：`billing.ts:19` 為 `Math.ceil(fee / Math.max(confirmedCount, 1))`，分母保底，K=0 時攤額即完整場地費。`billing.ts:13` 註解另記「主辦自動登記已保證 confirmedCount>=1，仍以 max(,1) 防除零」。
- **但 K=0 確實可達**（主辦開團後自行 `-1`；`event-formatter.ts:368` 與 D-017 AC-7 即為同一情境所設）。屆時輸出「（目前正取 0 人，平均每人約 4000 元；暫估，關閉報名後結算）」。
- **使用者裁決（2026-09-02）：採審查員給的修法之一（「若已 guard，補一條測試釘死 K=0 輸出字串」），維持現行文案不改。** 已補測試於 `event-formatter.edit.test.ts`（`[D-019 AC-1]` K=0 案）：斷言 `Number.isFinite` 為真、攤額 4000、整句字元級釘死，並一併釘死 D-017 AC-7 的「K=0 不輸出提示句」，避免日後只改其中一半。
- 此為審查員自列的兩個修法之一 ⇒ blocker 依其自身條件封閉，不需第二輪審查。

### 待處理（不阻塞本任務結案）
- **D-019 補 errata ×2**（architect 指定落點）：①§一.3 `feeLabel` 後加註「domain→formatter 執行期單向依賴為刻意接受；formatter 對 event-service 必須維持 `import type`，一旦需要 value import 即須抽共用模組」②AC-7 後半句改記實際做法（舊原語已移除）。
- **nit 已登記，見 §8。**

## 8. nit 清單（不阻塞，登記 `docs/backlog.md`）
1. **（architect n1，建議近期處理）** `event-repository.ts` 28-32 區塊註解仍寫「一次呼叫只動一欄……由原語形狀保證」，其下方現在放的是三欄的 `updateBilling`，**註解與程式碼互相矛盾**，會誤導後續讀者對 G2 的理解。
2. （architect n2）`updateBilling` 參數型別允許違反 D-005 的組合（`split_venue` 配 `venueFee: null`），不變式僅靠呼叫端維持；可改判別聯集讓型別直接擋。
3. （architect n3）`feeModeSwitched` 的條件展開等價於直接放欄位；審查包 §6 那兩條錯誤斷言即源於此設計。
4. （architect n4）AC-7 的 spy 用累計次數（1→2）斷言，改 `mockClear()` 後各自斷言 1 次更直白。
5. （design n1）per_person 在使用者可見文案中有三種說法：`每人費用`／`每人固定`／`每人固定費用`。改動需先改設計文件，建議日後以 errata 統一為「每人費用」。
6. （design n4）**產品面**：split_venue 活動輸入裸數字 `編輯 費用 4000` 會靜默切成 per_person。屬 D-019 明文採納的語法決議，且回覆句明示「場地費 3000 元 → 每人費用 4000 元」可自我修正，不阻塞。
7. （design n5）AC-8 測試僅斷言 `package.json` 有四條 script，屬同義反覆；沿用 D-014 AC-5 慣例，記錄即可。

## 9. 最終機器關卡（2026-09-02，補測試後複驗）
`npm run lint` ✓ 0 ／ `npm run build` ✓ ／ `npm test` ✓ **488 passed / 488**（62 files）／ `npm run harness:check` ✓ AC 249/249、三 check 全過。
