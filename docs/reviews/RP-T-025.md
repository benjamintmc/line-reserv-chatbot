# 審查包（Review Packet）— T-025 容器強化 M7

- 任務：T-025 ／ 設計：任務單內 stub（R0，無 D 檔，依 CLAUDE.md §5）／ 風險：R0
- 變更檔案清單（含行數增減）：
  - `Dockerfile`（+4 / -0）——runtime 階段新增 `USER node` 與 3 行註解
  - `.dockerignore`（**新增**，+41 / -0）
  - （不含）`docs/reviews/RP-T-025.md`、`docs/worklists/backend-engineer.md`：本次交付文件

## 1. 變更摘要（≤ 5 行）
1. 新增 `.dockerignore`，把 `.env*`／`node_modules`／`dist`／`data`／`.git` 等排除在 build context 外——
   docker build context 不看 `.gitignore`，這是唯一能讓 **build 階段也拿不到 `.env`** 的手段。
2. `Dockerfile` runtime 階段在 `CMD` 之前加 `USER node`（node:22-slim 內建 uid 1000）。
3. `CMD`、base image、build 階段既有指令**皆未改動**；未使用 `chown`／`chmod`。

## 2. Guardrails 自檢表
| Guardrail 條目 | 遵守？ | 證據（檔案:行） |
|---|---|---|
| 不得放寬檔案權限（`chmod -R 777` 等）或退回 root 執行 | ✓ | `Dockerfile:17-21` 僅新增 `USER node`，全檔無 `chmod`／`chown`／`--user root`；實跑 `id` = `uid=1000(node)`（見 AC-1） |
| 不得把任何 secret 寫進 image | ✓ | `.dockerignore:6-8` 排除 `.env` / `.env.*`；final image `find / -xdev -name ".env*"` **零命中**（見 AC-2）；`Dockerfile` 無 `ENV` 帶密值（僅 `NODE_ENV=production`） |

## 3. Acceptance Checks 對照
| AC | 驗證手段（R0 手動實跑，非 unit test） | 狀態 |
|---|---|---|
| [T-025 AC-1] `docker build` 成功、容器內 `id -u` 非 0、`/health` 回 200 | 見下方「AC-1 實際輸出」 | **PASS** |
| [T-025 AC-2] 最終 image 內不存在 `.env` | 見下方「AC-2 實際輸出」 | **PASS** |

> 註：兩條 AC 皆**無法**由 `npm test` 驗證（需真實 docker build/run），故依 D-000 模板新規以
> 「指令 + 原始輸出」貼證。`check_ac_coverage.py` 不會掃到 `[T-025 AC-n]`（R0 無 D 檔，非其掃描來源）。

### AC-1 實際輸出
```
$ docker build -t golf-reserv-chatbot:t025 .
...
#11 [build 5/5] RUN npm run build
#11 3.261 [postbuild] 已複製 migrations → /app/dist/db/migrations
#11 DONE 3.3s
#13 [stage-1 5/5] COPY --from=build /app/dist ./dist
#14 naming to docker.io/library/golf-reserv-chatbot:t025 done
#14 DONE 0.3s

$ docker run -d --name t025 -p 18080:8080 -e PORT=8080 \
    -e DATABASE_URL="postgres://fake:fake@127.0.0.1:5432/fake" \
    -e LINE_CHANNEL_SECRET=fake_secret -e LINE_CHANNEL_ACCESS_TOKEN=fake_token \
    golf-reserv-chatbot:t025
$ docker ps --filter name=t025 --format '{{.Names}} {{.Status}}'
t025 Up 5 seconds
$ docker logs t025 | tail -2
{"level":30,...,"msg":"Server listening at http://172.17.0.2:8080"}
{"level":30,...,"msg":"golf-reserv-chatbot 已啟動：http://127.0.0.1:8080"}

$ docker exec t025 id -u
1000
$ docker exec t025 id
uid=1000(node) gid=1000(node) groups=1000(node)

$ docker exec t025 node -e "fetch('http://127.0.0.1:8080/health').then(async r=>console.log('HTTP',r.status,await r.text()))"
HTTP 200 {"status":"ok"}
$ curl -s -w '\nHTTP %{http_code}\n' http://127.0.0.1:18080/health
{"status":"ok"}
HTTP 200
```
（`DATABASE_URL` 為假值；`/health` 不依賴 DB，故無需起 Postgres。驗後 `docker rm -f t025` 清掉。）

### AC-2 實際輸出
```
$ docker run --rm golf-reserv-chatbot:t025 ls -a /app
.
..
dist
node_modules
package-lock.json
package.json

$ docker run --rm golf-reserv-chatbot:t025 sh -c 'find / -xdev -name ".env*" 2>/dev/null; echo "[find done]"'
[find done]                      ← 全檔案系統零命中
```
額外驗證 `.dockerignore` 對 **build 階段**確實生效（M7 的真正風險面：`--target build` / cache export）：
```
$ docker build --target build -t golf-reserv-chatbot:t025-buildstage .
$ docker run --rm golf-reserv-chatbot:t025-buildstage sh -c 'ls -a /app; find /app -maxdepth 2 -name ".env*" -not -path "*/node_modules/*"; echo "[done]"'
.  ..  .dockerignore  CLAUDE.md  Dockerfile  README.md  dist  docker-compose.yml
eslint.config.mjs  node_modules  package-lock.json  package.json  requirements.md
scripts  src  test  tsconfig.json  vitest.config.ts
[done]                           ← build 階段亦零 .env（修復前此處會有 .env）
```

## 3.5 diff 範圍自檢（實作者交付前必做）
- [x] §3 AC 對照表點名的每一個檔案（`Dockerfile`、`.dockerignore`）都在本包所附 diff 中
- [x] 非 R2，但本任務受影響檔案僅此 2 檔，已全附；`git status` 另有 `docs/task-board.md`、
      `harness/OWNERSHIP.md`、`docs/backlog.md` 的既存改動，**非本任務產出**（Orchestrator 的未提交變更），未觸碰
- [x] diff 以檔案層級路徑產出（根目錄 2 檔），無逐檔手誤漏檔風險

### diff
```diff
--- a/Dockerfile
+++ b/Dockerfile
@@ -15,6 +15,10 @@ ENV NODE_ENV=production
 COPY package*.json ./
 RUN npm ci --omit=dev
 COPY --from=build /app/dist ./dist
+# 非 root 執行（T-025／資安盤點 M7）：node 官方 image 內建 uid 1000 的 node 使用者。
+# /app 與 node_modules 維持 root 擁有、僅供讀取執行——不放寬權限（G1）。
+# PORT 8080 > 1024，非特權使用者可綁定。
+USER node
 # PORT 由 Cloud Run 注入（預設 8080）；config.port = process.env.PORT ?? 3000 已相容。
```
`.dockerignore` 為新增檔（41 行），內容分區：env/secrets、node_modules、build output、
本機 DB 殘留、VCS/CI 設定（`.git`/`.github`/`.claude`）、文件目錄（`docs`/`design`/`harness`）、logs/OS。
**保留在 context 內**（build 需要）：`package*.json`、`src`、`scripts`、`tsconfig.json`、`test`。

## 4. 機器關卡結果
- [x] lint 通過（`npm run lint` exit 0，零 warning）
- [x] build 通過（`npm run build`：tsc + postbuild 複製 migrations）
- [x] unit test 通過（**371 passed / 371**，49 files；與 T-023 基線**完全相同，零變動零回歸**——本任務未改任何 TypeScript）
- [x] `npm run harness:check` 全過：`check_ac_coverage` 193/193、`check_doc_budget` 通過（8 份豁免）、
      `check_board_sync` 通過（**此為寫入 worklist 狀態提議前之結果**；提議寫入後該項會顯示
      「T-025 提議 → DONE，但 task-board 尚未裁定」，此為流程設計的預期訊號，待 Orchestrator 裁定即消）

## 5. 需要 reviewer 特別留意的地方（≤ 3 點）
- **`.dockerignore` 排除範圍大於任務單的最低要求**（任務單為「至少」排除 5 項）。我另外排除了
  `docs`/`design`/`harness`/`.github`/`.claude`/`test` 等純文件與流程目錄以縮小 context。已確認
  `tsconfig.json` 的 `include` 只有 `src`、`postbuild` 只讀 `src/db/migrations`，故 build 不受影響
  （已用實跑 build 成功佐證）。若希望嚴守最小改動，可刪除該區塊，不影響 M7 修復。
- **`.env.example` 也被 `.env.*` 一併排除**。它無敏感值，但 build 與 runtime 都用不到；若日後想在 image 內
  附帶範例檔，需加一行 `!.env.example`。
- **Cloud Run 影響評估**：`USER node` 後容器對 `/app` 只有讀取權。目前程式不寫入本機檔案系統
  （PG-only，`better-sqlite3` 已於 T-012 移除、`data/` 已無用），故無權限問題；實跑啟動與 `/health` 皆正常，
  未遇到任何權限錯誤。若未來新增本機寫檔需求，須改用 `/tmp`（Cloud Run 唯一可寫處），不得放寬 `/app` 權限。
