# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-07-31 12:30 / 使用者收工（M3 完成後「先休息 / 交接快照」）
- 目前 Phase：**M3 開團流程完成（T-008 DONE）**。M0~M3 全數完成，產品端到端可用（開團→報名→取消→候補→遞補→關閉/取消活動）。下一步待使用者選：M3 真機跨試 / M4 我的ID / M5 部署
- harness 版本：1.1.0（見 harness/VERSION）
- git：乾淨，最新 commit `c0c4bc6`（T-008）。分支 main，全程直接 commit 到 main（沿專案慣例）。

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| （無進行中實作）| M3/T-008 已 DONE 並提交；無半成品 | — | 由使用者選下一步（見第 5 節選項）|
| T-007 M2 跨試 | DONE；真實 LINE 群組驗證通過 | scripts/seed-open-event.ts, docs/integration-test-m2-line.md | 若做 M3 跨試：見下方「M3 真機跨試前置」|
| e2e（整合階段待辦）| 尚未做 | — | 發布前補 e2e：D-003 AC-17（主辦跨 owner 代取消觸發遞補）、D-004 AC-18（開團→報名銜接）|

## 2. 尚未落入文件的口頭決策
（本 session 所有裁決均已落入文件）
- D-004 OP-1~OP-9 全數定案並寫入 `design/D-004-event-creation.md` 討論紀錄（非白名單回提示、一行式仍需確認、任一白名單主辦可關/取消、draft 不物化、TTL/逃逸/日期採預設、validator 由 commands 匯出）。
- 部署方案 + 免費額度分析 + PG/serverless 遷移路線 → 已存 `docs/deployment.md`。

## 3. 已知技術債與風險（task-board Backlog 有完整清單）
- **M5 部署前必修**：`tsc` build 不複製 `src/db/migrations/*.sql` 到 `dist/`，生產 `node dist` migrate 會失敗（三解法見 deployment.md §6 / task-board Backlog）。
- **拒絕回覆 mark 政策不對稱**（LESSONS 已登記，**第 2 次出現＝回寫候選**）：D-003 nit-3 + D-004 §9 同型。下次階段回報時應向使用者提案回寫成統一政策（落 CLAUDE.md §4 或 handler 設計指引）。D-004 §9 已依實作校正（生命週期交易內一律 mark、交易前 early-return 不 mark）。
- **APPROVED 狀態行勿加 markdown 粗體**（LESSONS 已登記）：`check_ac_coverage.py` 的 regex 認不出 `狀態：**APPROVED**` → AC 漏檢假綠。D-004 曾因此假綠 58/58，改純文字後為 80/80。
- **環境**：本機 `python`/`python3` 是 Windows Store stub → harness Python 檢查一律用 `py`（`PYTHONIOENCODING=utf-8 py harness/checks/check_ac_coverage.py`）。better-sqlite3 pin `^12.4.1`（ADR-003）；vitest 首次冷跑偶發一次性 flake，重跑即綠。
- **LINE 平台**：bot 只用 reply（不吃 200 則 push 額度）；`getGroupMemberProfile`（單一成員）所有帳號可用，「取成員 ID 清單」才需 verified/premium（M4 若做「@全員/列未報名者」需注意）。跨試踩雷：tsx watch 不重載 .env（改 env 要重啟）、Verify 成功≠訊息送 webhook（需 Response mode=Bot）、Windows console 亂碼用 `chcp 65001`。

## 4. 阻塞與等待事項
- 無技術阻塞。build/test/lint 全綠（165 tests、AC 80/80、lint 0 error）。
- 等使用者選下一步方向。

## 5. 給接手者的第一句話
以 Orchestrator 身分報到，摘要「M0~M3 全部完成、M2 已真機驗證、M3（開團流程）R2 三關通過並提交」，然後問使用者要走哪條：**(A) M3 真機跨試開團**、**(B) M4 我的ID（需先寫 D-005 設計）**、**(C) M5 部署（Fly.io，先修 migrations 複製 bug）**。

**若走 (A) M3 真機跨試前置**（使用者要在 LINE 群裡打「開團」）：
1. 需把使用者 userId 加進 `.env` 的 `ADMIN_USER_IDS`（白名單才能開團）。使用者 userId＝`Ueecb1a9c984cbee7eb1aecd8d4f25854`（本 session 由 DEBUG_WEBHOOK log 取得；群組 groupId＝`Cfa4e8cbbfcf5b8c3f11e7815a69b448b`）。
2. 改 `.env` 後**務必重啟 `npm run dev`**（tsx watch 不重載 .env）。
3. 稍早 T-007 seed 過一場 open 活動（capacity=3）在 `data/golf.db`，會擋住新開團（「已有進行中活動」）；先在群裡打「取消活動」（需白名單身分）或刪 `data/golf.db` 重來。
4. 跨試序列：`開團 2026/08/15 07:30 東方球場 16人 2200元`（一行式）或 `開團`（逐步問答）→ 摘要 → `確認` → 群公告 → `+1`/`名單` → `關閉報名` / `取消活動`。對照 D-004 §8 範本 (A)–(M)。
5. `.env` 目前 `DEBUG_WEBHOOK=1`（本 session 設）；正式部署前關掉。cloudflared quick tunnel 每次重啟網址會變，需回 LINE Console 更新 Webhook URL。

**設計現況**：D-001/D-002/D-003/D-004 皆 APPROVED。D-005（我的ID，M4）、D-006+ 尚未撰寫。ADR-004 保留給「未來 PG 切換」（deployment.md §5 有完整遷移計畫），本 session 未動用。
