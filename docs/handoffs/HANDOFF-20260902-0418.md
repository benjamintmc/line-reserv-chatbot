# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-09-02 04:18 CST ／**本輪工作（D-020 切檔 + R2 雙審 + 修訂）已完成並 push**，使用者結束 session；下一輪直接 approve 後進入實作
- 目前 Phase：**Phase 4 實作階段**。D-020 已由單一巨型設計降級為 umbrella 索引，實作任務就緒待派工
- harness 版本：1.4.0（見 `harness/VERSION`）

## 0. 起手狀態（先讀這段）

- **工作分支 `docs/d-020-split-tasks`（HEAD `182bfc8`）已 push 到 origin**，**尚未開 PR**（使用者未要求）。
  開 PR 網址：`https://github.com/benjamintmc/golf-reserv-chatbot/pull/new/docs/d-020-split-tasks`
- 本分支自 `docs/deploy-v8-record` 分出，**故包含 `da52477`（:v8 部署紀錄）**。該 commit 也在
  `origin/docs/deploy-v8-record` 上；兩者若各自開 PR 合併 main，git 會自行處理重複，不需特別動作。
- `origin/main` = `7bebb08`。`origin/docs/d-020-split` 是**舊分支**（內容早已是 HEAD 的祖先），勿重用。
- **本機環境已改善**：`node v24.18.0`／npm 皆可用（前一份 handoff 記載的「本機無 node」已不成立）。
- **本輪機器關卡全部在本機實跑並通過**（非採信回報）：
  `lint 0`／`typecheck 0`／`build ✓`／**488 tests（62 檔）全過**／harness 四關卡全綠（AC 覆蓋 249/249）。
  本輪**零程式碼變更**，488 tests 與基線相同。

## 1. 進行中任務的中間狀態

| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-033 | **已拆，不直接派工**。父列保留供舊引用解析 | `design/D-020-multi-event-per-group.md`（umbrella） | 無。勿刪父列（`check_board_sync` 認不出 `T-033a`） |
| **T-033a** | **READY**，設計已 APPROVED + R2 雙審完成 | D-021／D-022／D-023／D-024／D-026 | 使用者 approve 後派 backend-engineer。**動工第一個動作：讀 `D-021` §一 末尾的〔切檔複審新增〕開團側過渡條文，與 `D-020` §一的「連帶測試窮舉表」**——這兩段決定了這批能不能綠燈交付 |
| T-033b | BLOCKED（待 T-033a） | D-025／D-029 | — |
| T-033c | BLOCKED（待 T-033a、T-033b） | D-027／D-028 | — |

**T-033a 動工必做的三件事**（漏掉任一項會直接卡住）：
1. **移除五份子文件的 `AC 覆蓋：**待動工豁免**` 那一行**（D-021/022/023/024/026），否則 17 條 AC 不受檢＝假綠。
2. **測試標記用子文件的 D 編號**，例如 AC-8 寫 `[D-023 AC-8]` 而**不是** `[D-020 AC-8]`——
   `check_ac_coverage.py` 的文件編號取自**檔名**。每份子文件 §三 開頭都有一行提示。
3. **7 處既有測試對舊索引名 `ux_events_active_group` 的斷言必須同批更新**（清單見 D-020 §一表格），
   否則 0006 DROP 後 `npm test` 必紅。

## 2. 尚未落入文件的口頭決策

**無遺漏**——本輪兩項使用者裁決皆已落檔：

| 口頭決策 | 已落入 |
|---|---|
| D-027 以「引用繼承 G8」滿足 CLAUDE.md §5「R2 至少 3 條 Guardrail」——**可接受**，不得湊數新增 Guardrail | `D-020` 討論紀錄 2026-09-02 列、`D-027` §二第 2 條、`task-board` T-033c 備註 |
| 20 行〔切檔新增〕內容**送 R2 雙審複審** | 已執行，審查包 `docs/reviews/RP-D-020-SPLIT.md`（含 §6 複審結果與處置） |

## 3. 已知技術債與風險

1. **⚠️ 最重要：R2 雙審的修訂內容本身未經第二輪複審。** 兩位 reviewer 合計 5 個 blocker，
   orchestrator 已逐條對程式碼核實並修訂，但修訂後的新條文——特別是 **`D-021` 的開團側過渡條文**
   （取 `actives.at(-1)`、宣告為 G1 的明示過渡例外）——只有 orchestrator 落筆，**沒有 reviewer 看過**。
   design-reviewer 明示「修完後可直接複驗此兩處」，architect-reviewer 未表態。
   **建議 T-033a 動工前，先請 architect-reviewer 單獨複驗該節**（約 15 行，成本很低）。
2. **`design/D-007-postgres-migration.md` 的 errata 列是 orchestrator 代筆補上的**（複審 B2 發現原表
   遺漏）。architect 尚未確認。該列本身只是登記「0006 後 `err.constraint` 斷言失效」，事實已由
   orchestrator 對 `d007-postgres.test.ts:171` 查證。
3. **兩份文件超過 R1 的 120 行提示線**（umbrella 146 行、D-021 133 行），`check_doc_budget` 會印 ℹ。
   **這是預期的、不是待辦**：R2 依 TOKEN-BUDGET 規則四不設上限，且 T-033a 已經雙審確認為不可再分的
   原子。**不得據該提示再拆**——理由已寫進 D-020 討論紀錄，避免下一位 agent 重新開戰。
4. **D-020 §0 原文「擴散到 6 個呼叫端」是低估**，實測 production 呼叫點 12 處（`event-service` 8、
   `grouping-service` 2、`registration-service` 2）。原文屬已核可內容故逐字保留，補正寫在 D-020 §一。
   **T-033a 的工時估計請以 12 處為準。**
5. `docs/proposals/D-020-SPLIT-PROPOSAL.md` 是**已被取代**的舊提案（5 任務／8 文件，有三處切在呼叫邊上）。
   已加註取代標記並移出 `design/`，**不得再據以派工**。權威版本是 D-020 §一。

## 4. 阻塞與等待事項

- **無技術阻塞。** 唯一等待項：**使用者 approve 後即可派 backend-engineer 動工 T-033a。**
- 未開 PR（使用者未要求）；若要合併 main，記得 `docs/deploy-v8-record` 的 commit 已含在本分支內。

## 5. 給接手者的第一句話

D-020 的切檔已經全部做完並經 R2 雙審修訂完畢——**你不需要再讀那份 729 行的原始設計，也不要重新
討論怎麼切**；現在最重要的一件事是：使用者 approve 後派 backend-engineer 實作 **T-033a**，而動工前
唯一該先做的是讀 `D-021` 的開團側過渡條文與 `D-020` §一的連帶測試窮舉表（並考慮先請
architect-reviewer 複驗前者，見 §3.1），因為那兩段決定了這批改動能不能在 `npm test` 綠燈下交付。
