# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-09-02 01:04 CST ／使用者換到另一台電腦接續，且**本機環境缺 node/npm**（不只是缺 docker），機器關卡完全無法在本機驗證，必須換有完整開發環境的機器
- 目前 Phase：**Phase 4 實作階段**。D-019、D-020 兩份 R2 設計皆已 APPROVED，一個實作完成待驗證、一個實作中斷待重啟
- harness 版本：見 `harness/VERSION`

## 0. 起手狀態（先讀這段）
- **兩個工作分支，皆尚未 push 到 origin**：
  - `feat/t-032-edit-billing-mode`（HEAD `e054ed0`）：含 D-019/D-020 兩份設計文件的 APPROVED 全紀錄、11 份既有文件 errata、T-034 實作（commit `fe142d8`）。
  - `feat/t-033-multi-event-per-group`（HEAD `79d7793`，**分支自 t-032 舊版，尚未套用 T-032→T-034 改號 commit**）：D-020 實作用分支，目前只有文件、**零程式碼變更**。
- `main`／`origin/main` 為 `12bd428`，乾淨、與 origin 同步。
- **⚠️ 遠端已存在 `origin/chore/t-032-typecheck-tests`**（另一 session 於 2026-09-02 00:41 建立，處理「測試檔假綠」backlog 項目，與本輪工作無關但**曾與本輪 T-032 編號撞號**——已將本輪改號為 T-034 排除衝突，`chore/t-032-typecheck-tests` 本身未動、未合併，合併 main 前需留意）。
- **本機環境限制（新發現，比預期嚴重）**：`which node npm` 找不到、全機找不到任何 node 執行檔，`npm run lint`／`build`／`test`／`harness:check` 全部無法執行。backend-engineer 只能做人工程式碼審閱，**機器關卡全數未跑**。跑測試前原本就需要 `docker compose up -d`（PG:5433），docker 同樣未裝。

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-034（D-019，計費模式切換） | **實作完成**，機器關卡未跑 | `src/domain/{event-service,event-formatter}.ts`、`src/db/repositories/event-repository.ts`（新原語 `updateBilling`）、`src/webhook/handler.ts`、+2 測試檔；commit `fe142d8`＋`e054ed0`（分支 `feat/t-032-edit-billing-mode`） | 在有 node+docker 的機器 checkout 此分支，跑 `docker compose up -d` 後依序 `npm run lint && npm run build && npm test && npm run harness:check`，四關全綠才能送 reviewer |
| T-033（D-020，同群多場並行活動） | **中斷，狀態不明確**——第一次派工於寫任何程式碼前就因 session 用量上限中斷（零變更）；第二次重新派工後**本 session 結束時仍在背景執行中（worktree locked）**，是否完成、有無 commit 未知，**背景 agent 不會跨機器/跨 session 存活** | 分支 `feat/t-033-multi-event-per-group`（HEAD `79d7793`，目前應仍是零程式碼變更），worktree 路徑 `.claude/worktrees/agent-ae82988942b18b426`（**此 worktree 屬於本機這個已結束的 session，換機器後無法接續，該資料夾可忽略/清除**） | 先 `git log feat/t-033-multi-event-per-group` 確認有沒有意外多出 commit（理論上沒有）；若仍是零變更，**直接重新派工 backend-engineer 依 `design/D-020-multi-event-per-group.md` 全文實作**（15 Guardrails／30 AC，範圍見該文件；務必先讀 9 份既有文件裡「來源 D-020」的 errata 區塊） |

## 2. 尚未落入文件的口頭決策
（無。T-032→T-034 改號、D-019/D-020 APPROVED 等本輪決策皆已寫入 `docs/task-board.md`／`docs/00-project-brief.md`／各自設計文件。）

## 3. 已知技術債與風險
- **T-033（D-020）範圍很大**：migration 0006、`EventReader` 介面全面改寫（`findActiveByGroup`→`listActiveByGroup`）、新增 `selector.ts`／`event-disambiguation.ts`、`message_event_map` 新表與寫入時機、三個 domain service 改動。**雙審時已抓到一個真實的跨群資料外洩缺口（B1）並已在設計文件層修正**（G14／`resolveQuotedEventInGroup`）——**實作時務必照設計文件的修法做，不要自己重新設計這段**。
- **`origin/chore/t-032-typecheck-tests`**：另一 session 的未合併工作，處理 backlog 已登記的「測試檔不受 tsc 型別檢查」問題。與本輪工作分支獨立、無檔案衝突，但合併進 main 的順序未定，需要使用者或下一位 orchestrator 裁決先後。
- **T-034 的一處設計文件字面規定**：`feeLabel` 依 D-019 §一.3/§一.5 字面規定放在 `event-formatter.ts`（非 `event-service.ts`），造成 domain 對 formatter 的一條 value import（反向為 `import type`，無執行期循環）。backend-engineer 已依文件字面實作、未擅自更動，**reviewer 審查時請留意這是唯一偏離「domain 不依賴 formatter」慣例的地方，且是設計文件明文指定**。
- **AC 覆蓋方法論偏離**：T-034 的 AC-7「未呼叫舊原語」改用「舊原語已整個移除、型別層面不可能呼叫到」佐證，而非額外寫 `not.toHaveBeenCalled()` 斷言（該方法已不存在）。reviewer 需認可這個等價性。

## 4. 阻塞與等待事項
- **最優先**：需要一台有 **node + npm + docker** 的機器/環境，才能：
  1. 對 T-034 跑四道機器關卡（lint/build/test/harness:check）。
  2. 重新派工並驗證 T-033。
  3. 兩者機器關卡全綠後，才能依 CLAUDE.md §6 送 design-reviewer + architect-reviewer 做**實作階段**雙審（設計階段的雙審已各自 PASS，但那審的是設計文件本身，不是最終程式碼）。
- 在此之前，**不要**嘗試用其他方式（例如遠端 CI）繞過本機驗證後就直接送審或合併——依 CLAUDE.md §6 第 0 條，機器關卡未全綠不得送模型審查。

## 5. 給接手者的第一句話
先確認自己所在的機器有 node/npm/docker，`git fetch` 後 checkout `feat/t-032-edit-billing-mode`（T-034 已實作完成，只差四道機器關卡——這是最快能推進的一步）；`feat/t-033-multi-event-per-group` 目前大機率仍是零程式碼變更，需要重新派工 backend-engineer 依 `design/D-020-multi-event-per-group.md` 從頭實作，不要假設有背景進度可以接續（背景 agent 綁在前一個 session，換機器後不會存活）。兩個工作分支都還沒 push 到 origin，接手後第一步請自行 push（`git push -u origin feat/t-032-edit-billing-mode` 與 `feat/t-033-multi-event-per-group`）以便留存。
