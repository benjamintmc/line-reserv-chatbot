# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

- 日期 / 交接原因：2026-08-31 15:36／使用者要求把 D-019 派工移入 backlog、寫交接後告一段落
- 目前 Phase：PROD LIVE（image `:v7`／revision `00009-8zt`，migration 0001–0005，477 tests／harness AC 242/242 全綠，無未結阻塞項）
- harness 版本：1.4.0

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-032 | `編輯 費用` 擴充支援切換計費模式（per_person ↔ split_venue）。**設計已完成並雙審 PASS**，但**尚未實作、尚未 APPROVED**——使用者要求先自行通讀設計全文再核可，本 session 到此暫停。已從 task-board 看板移入 `docs/backlog.md`（條目：「D-019 `編輯 費用` 支援切換計費模式——待使用者核可 APPROVED」）。 | `design/D-019-edit-billing-mode.md`（85 行，R2，architect-reviewer + design-reviewer 雙審皆 PASS、零 blocker，4 nit 已回填） | 使用者讀畢 D-019 後：①若核可 → orchestrator 把該列移回 task-board 看板、標記 D-019 APPROVED、派工 backend-engineer 實作（含回填 D-015 errata 與 `docs/02-api-contract.md`，清單見 D-019 §四）②若要修改 → 回 architect 修訂後重新送審。**分支 `feat/t-032-edit-billing-mode` 已建立，僅含 task-board 異動，尚無程式碼變更**，可直接繼續使用或視需要重建。 |

## 2. 尚未落入文件的口頭決策
（無——D-019 相關的三項使用者裁決已於本次 session 內全數寫入 D-019 本文與討論紀錄：①任何時候皆可切換計費模式，含已有人報名後，不限主辦一人時②不新增主動推播，沿用既有 reply 顯示改前→改後③mention 行為維持現狀，一律 @ 正取者、不為此新增例外）

## 3. 已知技術債與風險
- **本 session 發現一起 subagent 異常事件（務必記錄，非使用者操作）**：在「請 architect 套用雙審 4 nit 到 D-019」的 resume 呼叫中，該 agent 除了正確修改 `design/D-019-edit-billing-mode.md` 外，**額外且未被要求地**寫入了一段約 1700 字、完全無關的內容到 `docs/00-project-brief.md`（虛構「FR-8 同群組多場並行活動」功能、「決策 #9」，且**自稱設計文件為 `D-014`——與現有已上線的 D-014「DB TLS 驗證顯式化」直接編號衝突**，內部亦不自洽）。Orchestrator 當場發現（git diff 意外顯示非預期檔案異動）並已用 `git restore docs/00-project-brief.md` 撤銷，**未進入任何 commit**。`docs/00-project-brief.md` 現況與 `origin/main` 一致、乾淨。**風險**：無法排除同類狀況會再發生（agent 在被要求做局部修改時，逾越指定檔案範圍寫入其他檔案）；建議往後每次 agent 完成寫入任務後，orchestrator 一律先跑 `git status`／`git diff --stat` 核對「異動檔案清單是否等於指派範圍」，再決定要不要保留，不要只信任 agent 自述的「已修改路徑」。本事件本身未寫入 LESSONS.md（屬單次 session 內已攔截的異常，未造成實際損害；若日後重現視情況再登記）。
- `docs/backlog.md` 累積條目持續增長，尚未有切檔計畫（目前不構成問題，該檔本就設計為長期登記簿）。
- D-019 §四列出的「將改動的既有文件」（D-015 errata 六項、`docs/02-api-contract.md`）**尚未執行**——這些是 T-032 APPROVED 後、backend-engineer 實作階段的一部分，不是獨立待辦，不要重複建任務。

## 4. 阻塞與等待事項
- **等待使用者核可 D-019**（T-032 動工的唯一前提）。無其他阻塞。

## 5. 給接手者的第一句話
現在最重要的事：**不要重做 D-019 的設計或審查**——它已經寫好、已經雙審 PASS、四個 nit 也已回填完畢，唯一卡住的是使用者本人要通讀全文再點頭。你要做的只有兩件事之一：使用者核可了 → 把 T-032 從 `docs/backlog.md` 搬回 `docs/task-board.md` 看板、標 D-019 APPROVED、派工 backend-engineer（在 `feat/t-032-edit-billing-mode` 分支上）；使用者有修改意見 → 帶著意見回 architect 修 `design/D-019-edit-billing-mode.md`，改完照 R2 規矩重新雙審。另外，本檔第 3 節記了一起 subagent 誤寫 `docs/00-project-brief.md` 的事故（已攔截撤銷、無損害），純粹留供你提高警覺，不需要任何後續動作。
