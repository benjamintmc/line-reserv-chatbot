# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-09-02 17:39 CST ／**T-033b 已實作、R2 雙審封閉、合併、上線 `:v10` 並記錄完畢**，使用者結束 session
- 目前 Phase：**PROD LIVE**（image `:v10`／revision `00014-npk`）。Phase 4 實作階段，**T-033c 待派工**
- harness 版本：1.4.0（見 `harness/VERSION`）

## 0. 起手狀態（先讀這段）

- **`main` = `7bcd6b2`，已 push，工作區乾淨。所有分支已合併，沒有待處理的 PR。**
  （PR #25 = T-033b，以 **merge commit `d89a572`** 併入；分支 `feat/t-033b-quote-message-event-map` 保留未刪。）
- **PROD 跑的就是 `main`**：image `:v10`（digest `sha256:2bf25ae5`）／revision `00014-npk`／100% 流量／
  migration 0001–**0006**（**本批無新 migration**）。回滾點 `:v9`／`00013-pxr`，**無 schema 顧慮**。
- **repo 已改名為 `benjamintmc/line-reserv-chatbot`，本機 remote URL 已於本 session 更正**
  （前一份交接列為待辦，現已完成並實測 `git fetch` 不再出現 `repository moved` 警告）。
  GCP 端座標**不受影響**：Artifact Registry repo 仍是 `golf-reserv`、Cloud Run service 仍是 `golf-reserv-chatbot`。
- **本機環境**：`node v24.18.0`／npm／docker 29.6.2／`gcloud`（專案 `group-chatbot-504305`）皆可用。
  **四關可在本機實跑，不要採信 subagent 回報。**
- **現況全綠（2026-09-02 由 orchestrator 於本機親自實跑）**：
  `lint 0`／`typecheck 0`／`build ✓`／**537 tests（68 檔，零 skip）**／harness **AC 273/273**、
  `doc_budget ✓`、`board_sync ✓`。CI 於 PR 與 main 皆綠。
- **⚠️ 本 session 有兩項工具權限被 auto mode 攔下**（不是指令寫錯，重試無用）：
  ①部署冒煙的「pooled 連線真實查詢」（會讀 `database-url` secret）；
  ②`git remote set-url`（後由使用者於 terminal 自行執行）。
  接手者若需這兩類動作，**請直接告知使用者要權限，不要繞道**。

## 1. 進行中任務的中間狀態

| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-033a | **DONE**、已上線（`:v9`） | 審查包 `docs/reviews/RP-T-033a.md` | 無。**真機驗證刻意不做**，見 §2.1 |
| T-033b | **DONE（2026-09-02）、已上線（`:v10`）**。R2 雙審 B-1 已封閉 | 審查包 `docs/reviews/RP-T-033b.md`；產出路徑見 task-board | 無。**唯二未驗項見 §3.1**，已併入 T-033c 後的真機驗證 |
| **T-033c** | **READY**（2026-09-02 因 T-033b DONE 而解除阻塞），設計 APPROVED | `design/D-027`（開團查重）、`design/D-028`（同群 open 上限 3 場） | 使用者 approve 後派 backend-engineer。**動工第一個動作**：讀 D-027／D-028 三段式，並處理 §1.1 的四項前置 |

### 1.1 T-033c 動工前必須知道的四件事

1. **必須移除兩份設計文件的「待動工豁免」宣告**。`check_ac_coverage.py` 目前對 D-027（3 條 AC）、
   D-028（3 條 AC）印 `⏸ 待動工豁免（未計入）`，**動工時須移除該宣告**，否則 AC 覆蓋是假綠。
   （T-033b 動工時踩過同一步，照做即可。）
2. **必須補 `duplicate_event` 的 `relatedEventId` 錨點**（**D-029 errata E1 已明文要求**）。
   T-033b 的 `renderCreateEntry/already_active` 分支依相位**刻意不附錨點**，
   T-033c 把它改名為 `duplicate_event` 時**必須補上**，否則使用者引用該則訊息會失效。
3. **必須移除 D-021 的開團側過渡條文**（開團入口仍擋第二場的那段）——那是 T-033a~b 的過渡狀態，
   T-033c 開燈後即失效，不移除會變成互相矛盾的規範。
4. **必須交付 `docs/manual-test-T-033.md`**（完整真機測試腳本）。**這是 T-033c 的 DONE 條件，非選項**，
   規格見 §2.1。**未附合格腳本 = 未完成。**

## 2. 尚未落入文件的口頭決策

**本 session 無新的口頭決策未落檔。** 使用者僅下了兩道執行指令（「先 push+create pr+merge+deploy v10」、
「`git remote set-url` 改成正確的」），皆已執行並記錄於 `docs/deployment-runbook.md` 附錄與 task-board。

**唯一仍只存在於交接文件的規格，是下面 2.1 的測試腳本要求**（裁決本身已在 task-board，但腳本細則只在此）。
以下逐字沿用前一份交接，**不得因為換手而稀釋**：

### 2.1 ⚠️ 真機驗證延後 + 完整測試腳本（使用者明確要求，**非選項**）

**裁決（2026-09-02）**：**真機測試等到功能完整（T-033c 上線後）再一次做完**，且交付時**必須提供完整測試腳本**。

**理由**：多場並行在 T-033c 之前不對使用者開燈，此刻只能驗單場零回歸——等於同一批人要為同一個功能被拉去測三次。

**連帶規則**：
- **T-033a／T-033b 各自上線後不再要求真機驗證**，改由機器關卡 + R2 雙審把關（兩批皆已照此執行）。
- `真機驗證` 的 DONE 條件**統一掛在 T-033c**；**T-033c 未附合格腳本 = 未完成**。
- **現在不要去催使用者測。**

**腳本規格（使用者要求「完整」，請照這個標準做）**：

1. **可照著一路打完，不需回頭讀任何設計文件。** 每一步明確寫出**要打的字**（逐字、可複製）與
   **預期回覆**（逐字、含標點）。不要寫「檢查是否正常」這種要讀者自己判斷的句子。
2. **含前置設定**：需要幾個 LINE 群組、幾個帳號（主辦／一般成員／非授權者／super-admin）、
   每個群組要先開幾場什麼樣的活動（場地／日期／時間要指定，因為 `@selector` 的比對規則吃這些）。
3. **涵蓋 T-033a~c 全部功能**，至少包含：
   - **單場零回歸**：`名單`／`+1`／`-1`／`開團`（逐步 + 一行式）／`編輯 日期·時間·場地·費用`／
     `分組`→`下一輪`／多行批次。**含 `@隨便打的文字 +1` 在單場時應照常成功**（AC-6，最易寫錯處）。
   - **多場並行**（T-033c 開燈後才可測）：無指定 → ambiguous 提示；`@場地` 命中一場；`@場地 日期` 組合；
     日期不夠精準時加時間窄化；命中 0 場／>1 場的兩種錯誤回覆。
   - **quote（引言）**：**T-033b 已上線，現在真的會生效**。引用某則 bot 訊息 + `+1` 應作用到該場。
     **這是 PROD 上唯一還沒被真人跑過的新路徑，務必排在腳本前段。**
   - **授權作用對象**（AC-23）：A 場 host 甲、B 場 host 乙，甲下 `@B場地 取消活動` 應回非授權。
   - **開團查重與上限**（T-033c）：同場地+時間重複開團被擋；同群 open 第 4 場被擋。
   - **跨群不外洩**：引用他群訊息應視為未引言，不得洩漏他群資訊（G14）。
4. **每項標註對應的 AC 編號**（子文件編號，如 `[D-023 AC-8]`），讓失敗時能直接對回設計。
5. **標明哪些項目在哪個相位才可測**（T-033a 可測／T-033b 後／T-033c 後），避免功能還沒開燈就被誤判為 bug。
6. **附「預期不會發生什麼」**：例如單場時不該出現任何消歧義提示、`下一輪` 不該要求 `@selector`。

## 3. 已知技術債與風險

### 3.1 ⚠️ 最需要注意：`:v10` 有兩項未驗，我沒有當作驗過

1. **`message_event_map` 的 INSERT 路徑在 PROD 尚未跑過。** 要有真實回覆才會寫入，冒煙的空 `events` 打不到。
   **這是 T-033b 唯一沒被生產環境碰過的新程式碼路徑**，屬真機驗證範圍，已併入 T-033c 後那次。
2. **runbook §4.4 第②項「pooled 連線真實查詢」本次未執行**——工具權限攔截（見 §0）。
   本批未動連線字串與憑證，`:v9` 的同項驗證仍有效、`0006` 已 post-flight 確認，故風險可控但**確實是缺口**。

其餘冒煙四項全過：`/health` 200、未簽章 401、錯誤簽章 401、**帶正確簽章 200**、startup probe 一次即過、
revision log 無 ERROR（唯二 WARNING 為冒煙自身的 401）。

### 3.2 承接自前一份交接、仍然有效的項目

3. **`check_commit_trace.sh` 不吃帶字母後綴的任務編號。** pattern 是 `T-[0-9]+\)`，故實作型 commit 只能寫
   `T-033`（`docs:` 型走寬鬆分支故可用 `T-033b`）。**T-033c 會踩到同一個坑**，未裁定要不要改腳本。
4. **合併策略：T-033 系列用 merge commit，不要 squash。** T-033b 的文件硬引用了 `41c15fc`（D-026 errata）與
   `a8b9b09`（B-1 修正）兩個 sha，**squash 會讓它們變成不可達**。PR #23/#24/#25 皆為 merge commit。
5. **`close/cancel` 的交易內重讀實際上沒有列鎖**（`tx.ts:44-48` 明標不鎖 event；帶 `FOR UPDATE` 的
   `createImmediateRunner` 只有 `editEvent` 在用）。D-021 已補 errata 更正。
   **「兩次查詢不得合併」仍有效**，但不得再宣稱有 `FOR UPDATE` 等級的併發保證。
6. **去重政策的例外為兩類**（`CLAUDE.md` §4）。新增分支須在設計文件明列，**不得默默擴大**。
7. **⚠️ 審查意見不可全盤採信——累計三次實例。** 前兩次見 `HANDOFF-20260902-1234.md` §3.6
   （design-reviewer B-1 稱無先例，不成立；architect-reviewer 稱 D-021 clause 4 矛盾，後自行撤回）。
   **本輪第三次方向相反**：architect-reviewer 的 **B-1 經逐條查證屬實、不是誤判**（見 §3.3）。
   **⇒ 原則不變也不加碼：BLOCK 不代表對，也不代表錯，一律自己查證後再動手。**
8. **`@` 必須是第一個 token**（`selector.ts:87`），`+1 @旭陽` 會靜默落回 ambiguous 提示。已列 backlog。
9. **本機行尾 CRLF/LF 不一致**：committed 內容都是 LF、git 端無影響，只是本機 diff 會噴 warning。
10. **部署紀錄漂移已發生兩次**：①`00011` vs `00012` 的 revision 漂移；
    ②**`:v9` 只寫進 runbook §2.2 與看板，附錄「現行 tag」停在 `:v8`**（本 session 部署 `:v10` 時一併補登）。
    **每次 deploy 後請務必同時回寫 task-board 頁首與 runbook 附錄兩處。**
11. **三份文件超過 R1 的 120 行提示線**（D-020 146、D-021 143、D-025 142），`check_doc_budget` 會印 ℹ。
    **這是預期的、不是待辦**：R2 依 TOKEN-BUDGET 規則四不設上限。**不得據該提示再拆。**
12. **task-board 的 DONE 上限是 10 筆，超過 harness 直接紅（exit 1）。** T-033b 標 DONE 時踩到，
    已歸檔 T-026。**T-033c 標 DONE 時會再次踩到**，屆時把最舊一筆歸檔進 `docs/task-board-archive.md`。

### 3.3 本 session 新增：B-1 的實質內容（T-033c 動工前務必理解）

**quote 解出的 `eventId` 刻意不過濾候選集合**（D-026 §4.3：該場能不能做這件事，交給各指令自身的狀態判斷）。
⇒ **T-033b 起，各指令首次會收到非 active 的活動**。`+N`／`-N`／`加開`／`編輯`（含 `edit_help`）原本就有守門，
但 `名單` 與 `分組` 沒有，已於 `a8b9b09` 補上（`DISPLAYABLE_EVENT_STATUSES`／`isActiveEvent`）。

**這條規則對 T-033c 仍然適用**：D-027／D-028 若新增任何吃 `eventId` 的指令或分支，
**必須自己判狀態**，並更新 `design/D-025` errata E1 的逐指令盤點表（現涵蓋 `NEEDS_EVENT_SET` 全 9 成員）。

## 4. 阻塞與等待事項

- **無技術阻塞。無待處理 PR。無未結的機器關卡。**
- 唯一等待項：**使用者 approve 後即可派 backend-engineer 動工 T-033c。**
- `真機驗證` 依 §2.1 掛在 T-033c，**現在不要去催使用者測**。
- **待使用者裁決的產品問題（已入 backlog，不要自己決定）**：
  **「關閉報名後是否應該還能分組」**。現況維持 T-033b 前語意＝不能（`分組` 只吃 `{draft, open}`）。
- 退版限制（若需回退 0006）：重建 `ux_events_active_group` 前必須先確認
  `SELECT group_id FROM events WHERE status IN ('draft','open') GROUP BY group_id HAVING count(*)>1` 為空，
  否則重建直接失敗。程序見 `docs/deployment-runbook.md` §2.2。
  **回退 `:v10` → `:v9` 本身無 schema 顧慮**（本批無 migration）。

## 5. 給接手者的第一句話

T-033b 已經實作、R2 雙審封閉、合併、上線（`:v10`／`00014-npk`）並記錄完畢，`main` 就是 PROD、四關全綠、
沒有任何待處理項目——**你不需要回頭驗 T-033b，也不要去催真機測試**（使用者已裁決延到 T-033c 一次做完，
見 §2.1，而那份完整腳本是 T-033c 的交付條件之一）；現在最重要的一件事是：使用者 approve 後派
backend-engineer 實作 **T-033c**（D-027 開團查重 + D-028 同群 open 上限 3 場，R2，兩份必須同批交付），
動工前先處理 §1.1 的四項前置——**尤其是移除兩份設計的「待動工豁免」宣告**（不移除等於 AC 覆蓋假綠）
與**補上 `duplicate_event` 的 `relatedEventId` 錨點**（D-029 errata E1 已明文要求，漏掉會讓使用者引用該訊息失效）。
另外請記得兩件事：**`message_event_map` 的寫入路徑在 PROD 還沒被真人跑過**（§3.1，排進腳本前段），
以及**審查意見一律自己查證再動手**——本專案累計三次實例，兩次是誤判、一次（本輪 B-1）是真的（§3.2 第 7 點）。
