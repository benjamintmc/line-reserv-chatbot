# HANDOFF — 交接快照

> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**
> 取代 `HANDOFF-20260805-0200.md`（那份只涵蓋到 PR #3 合併前）。

- 日期 / 交接原因：2026-08-05 02:52 ／ 使用者裁定「目前版本先到這邊」，session 收尾
- 目前 Phase：PROD LIVE 維運中。本輪全部是**框架與規格工作，未動任何產品程式碼**
- harness 版本：**1.4.0**（見 `harness/VERSION`，含三項在地化偏離）

## ⚠️ 先看這個：有兩個 PR 開著沒合併

`main` 目前**不包含**下列內容。接手前先確認要合併還是繼續在分支上做，否則會做重工。

| PR | 分支 | 內容 | CI |
|---|---|---|---|
| **#5** | `docs/my-golf-spec` | 「我的球聚」規格定案（5 項裁決 + 功能意圖） | 綠 |
| **#6** | `docs/backlog-h1-h2` | H1/H2 列入 Backlog + P1 push 費用研究 + LINE 平台限制移入 01-architecture | 綠 |

兩者互不衝突，皆以 `main` 為 base。**本快照描述的是「#5 + #6 都合併後」的預期狀態**，並逐項標明目前落在哪裡。

## 1. 進行中任務的中間狀態

| 任務 | 進度 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-016 harness 升級 | **DONE**（已在 main） | `harness/`、`.claude/agents/`、`CLAUDE.md`、`docs/01-architecture.md`、`docs/02-api-contract.md`、`.github/workflows/ci.yml`、`scripts/harness-check.mjs` | 無 |
| Backlog 假待辦清理 | **DONE**（已在 main） | `docs/task-board.md` | 無 |
| 「我的球聚」D-009 | **規格定案，未動工**（在 **PR #5**） | task-board Backlog 該條 | 合併 #5 → 派 architect 寫 `design/D-009`。規格已全定，無待裁決事項 |
| H1 開團後加開名額 | **已登記，未設計**（在 **PR #6**） | 同上 | 需 `design/D-010`（R2） |
| H2 關閉報名時 @正取者 | **已登記，未設計**（在 **PR #6**） | 同上 | R1。**動工前需先裁決**：正取人數超過 mention 上限時的降級行為 |
| P1 開球前提醒 | **僅完成前期研究，未決定是否實作**（在 **PR #6**） | `docs/01-architecture.md`「訊息費用結構」 | 先實測「非好友能否被個別推播」（見 §4） |
| T-017 LESSONS 回寫清償 | **BACKLOG** | – | 讀 `harness/LESSONS.md` 問題登記表，把 5 項達門檻項目各寫成「建議措辭 + 落點」交使用者逐條核可 |

## 2. 尚未落入文件的口頭決策

**無。** 本輪所有裁決都已寫入 task-board 或 01-architecture，包括：

- 「我的球聚」五項裁決（不顯示群組名／代報名一併呈現／`YYYY-MM-DD HH:MM {{場地}} {{代報名|自己}} {{人數}}人`／日期分隔符對齊既有 `-`／候補不列出／不在群組內生效）
- 使用者定調的**功能意圖**：「列出所有已經確定的球敘，讓使用者知道自己該去哪幾場即可」——這句已寫入條目，它同時解釋了候補為何排除（候補不算確定），**接手者請勿把它當缺口順手補上**
- H1 只加開不縮減；H2 單則訊息、代報名只 tag 報名者本人
- H3（複製上一場）／P2（取消安全網）／P3（候補能見度）**經裁決不列入 Backlog**，本快照僅為紀錄

## 3. 已知技術債與風險

- **產品面**：6 項，索引在 `docs/01-architecture.md`「【技術債】現況清單」，交叉指向 task-board Backlog。最該優先的是**拒絕回覆的去重政策不對稱**（同型問題已 3 次，會讓使用者看到重覆回覆）。
- **框架面**：`check_doc_budget` 對導入日前既有的 8 份 D 文件豁免（`harness/doc-budget-exempt.txt`）；**新增的設計文件不得列入該檔**，120 行上限對它們仍有效。
- **task-board 已達 79/80 行**（PR #6 合併後）。再加任何 Backlog 條目都會被 `--strict` 擋下，屆時需再歸檔或壓縮。這是刻意的壓力機制，不是故障。
- **P1 若實作，會打破一個目前成立的架構前提**：系統至今只用 `replyMessage`（不計費）。主動提醒必須用 `push`/`multicast`（計費，且**推播到群組是按群組總人數計**）。詳見 01-architecture「訊息費用結構」。

## 4. 阻塞與等待事項

- **無阻塞**。工作區乾淨，所有 commit 皆已推上遠端。
- **待實測（P1 前置）**：個別推播是否要求對方已將官方帳號加為好友。此點決定提醒能否覆蓋全部報名者——若非好友推不到，P1 的價值會大幅縮水。**建議用真帳號實測後再談設計。**
- **待裁決（H2 前置）**：正取人數超過 mention 數量／訊息長度上限時的降級行為。

## 5. 給接手者的第一句話

**先處理那兩個開著的 PR**（#5、#6），否則你讀到的 task-board 會少掉本輪一半的決策。

之後有三條路，依價值排序：**T-017 LESSONS 回寫清償**（這套 harness 唯一從未運轉過的機制，欠款裡「拒絕回覆去重」和「errata 治理」各已出現 3 次，是真的在反覆咬人）、**D-009 我的球聚**（規格已定可直接動工）、**D-010 H1 加開名額**（使用者痛點最尖銳的一項——現況沒有任何編輯指令，唯一 workaround 是取消重開，會讓報名全部歸零）。

最後一件事：本輪修好了三個會產生錯誤訊號的 checks，其中兩個是**假綠**。請記得 `npm run harness:check` 的綠燈現在才是可信的——但也因此，**不要只看它有沒有通過，偶爾要驗證它是否真的會失敗**（LESSONS 2026-08-05 已登記此教訓）。
