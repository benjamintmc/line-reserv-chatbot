# HANDOFF — 交接快照

> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。** 接手請只讀本份 + task-board。

- 日期 / 交接原因：2026-08-18 14:19 CST ／ 雲端 session **無 node/npm/docker**，三個新功能已實作但一次都沒跑過機器關卡，交接給**有工具鏈的家中機器**跑驗證與 merge。
- 目前 Phase：PROD LIVE 維運中。本輪三個新功能實作於分支 `feat/D-011-grouping`（已 push 到 GitHub），**尚未驗證、未併入 main**。
- harness 版本：見 `harness/VERSION`。

## 0. 起手狀態
- 分支 **`feat/D-011-grouping`** 已 push 到 `origin`（GitHub）。`main` 乾淨、停在 `4338a70`。
- 本分支 commit（新→舊）：D-012 多行報名實作 → D-010 加開名額實作 → D-010/D-012 APPROVED+登記 → D-011 errata → D-012 設計 → D-010 設計 → D-011 分組實作。
- **所有程式碼皆在無工具鏈的雲端 session 寫成；`npm run lint/build/test`、`docker`、`harness:check` 一次都沒跑。** 已做的僅靜態核對（型別/union 窮舉/無 any/括號平衡/AC 標記靜態掃描 `check_ac_coverage` 綠）。

## 1. 最重要——家中第一件事：跑驗證關卡
```bash
git fetch origin && git switch feat/D-011-grouping   # 或 clone 後切分支
npm ci
docker compose up -d                                  # PG 測試 DB（port 5433）
npm run lint && npm run build && npm test && npm run harness:check
python3 harness/checks/check_ac_coverage.py
```
- 預期：全綠。測試涵蓋 **D-011 AC-1..24、D-010 AC-1..8、D-012 AC-1..9**。
- 任何一步紅 → 貼輸出回報 orchestrator 修（勿自行硬改設計）。

## 2. 高風險盯點（最可能出事處）
1. **D-010（R2）動到兩處凍結區鄰近路徑**：`src/domain/event-service.ts`（`canManageEvent` 改委派新 `authz.ts`）、`src/domain/registration-service.ts` 的 `cancel()`（鎖內遞補抽為私有 `promoteWithinLock`，與 `addCapacity` 共用）。工程師宣稱逐行等價、`src/db/tx.ts` 未動——**務必確認既有 signup/cancel/遞補/超賣測試零回歸**（`registration-service*.test.ts`、`event-service*.test.ts`）。
2. **D-011 AC-7/AC-8**（20 人 5 場零重複隊友/對手）靠 seeded 隨機搜尋（`RESTARTS`=2000、seed 42）；若 flaky → 調 `grouping.ts`/測試的 restarts 或 seed 再跑。
3. **D-012 邊界**：單行尾端換行（如 `+3\n`）會被 `/\r?\n/` 拆成 2 元素 → 走批次路徑（設計字面行為）；批次內全非成功原因 → 顯示最後一筆原因一次。確認可接受，否則回報微調。

## 3. 測試綠之後的審查關卡（DoD §6）
- **T-018 分組（R1）**：設計已雙審 PASS、實作已靜態雙 PASS → 補一位 reviewer 確認即可。
- **T-019 加開（R2）**：設計已 R2 雙審 PASS；**實作須 architect-reviewer + design-reviewer**（重點：authz/`promoteWithinLock` 抽取、`event-service`/`cancel` 改動、超賣守恆）。
- **T-020 多行（R1）**：設計已 design-reviewer PASS；**實作須一位 design-reviewer**（批次回覆文案/釘死字串）。
- 依 `harness/REVIEW-PACKET-TEMPLATE.md` 產 `docs/reviews/RP-T-018/019/020.md`。

## 4. Merge
- 三者測試綠 + 各自審查通過 → `git switch main && git merge feat/D-011-grouping` → push；orchestrator 於 task-board 標 T-018/T-019/T-020 DONE。

## 5. 給接手者的第一句話
先在有工具鏈的機器把整套測試跑綠——尤其盯 D-010 對 `cancel()`/`event-service` 的改動有沒有讓既有超賣/遞補測試回歸；綠了再走 R1/R2 審查與 merge。三份設計（D-010/D-011/D-012）皆 APPROVED，規格不需再議。
