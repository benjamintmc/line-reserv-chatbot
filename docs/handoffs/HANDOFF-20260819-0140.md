# HANDOFF — 交接快照

> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。** 接手請只讀本份 + task-board。

- 日期 / 交接原因：2026-08-19 01:40 CST ／ **Anthropic API 整晚間歇 529 Overloaded，連續 6 個 subagent 任務失敗**，使用者裁定 D-013 相關工作停手、明日續做
- 目前 Phase：PROD LIVE 維運中。三個新功能 + 一個使用者回報 bug 已驗證完成於分支 `feat/D-011-grouping`，**尚未併入 main**（依使用者裁決與 T-022 一併 merge）
- harness 版本：見 `harness/VERSION`

## 0. 起手狀態（先讀這段）
- `main` = `adfa6a0`，已含 **PR #11**（測試日期炸彈修復）。
- 工作分支 **`feat/D-011-grouping`** = `e1dadff`，**已 push**、工作區乾淨。
- **機器關卡全綠**：`npm run lint` 0、`npm run build` 綠、`npm test` **358 passed (46 檔)**、`npm run harness:check -- --strict` 全過（AC 184/184）。跑測試前須 `docker compose up -d`（PG:5433）。
- **不要重跑已完成的驗證**；也不要重做 T-018/019/020 的審查（已 PASS，審查包在 `docs/reviews/`）。

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-018 分組 | **DONE**（design 複審 PASS，B1/B2 封閉） | `src/domain/grouping*.ts` 等 | 無（等 merge） |
| T-019 加開名額 | **DONE**（R2 雙審 PASS 零 blocker） | `src/domain/{registration-service,authz}.ts` 等 | 無（等 merge） |
| T-020 多行報名 | **DONE**（blocker B1 已修 + D-012 errata 背書） | `src/webhook/handler.ts`、`list-formatter.ts` | 無（等 merge） |
| T-021 跨群 bug | **VERIFIED，有一項審查缺口** | `handler.ts`、`event-service.ts`、`grouping-service.ts`、`event-formatter.ts` | **重派 design-reviewer 審 (N2) 告知句**（唯一未完成的關卡，見 §4） |
| **D-013** PK 變更 | **IN_DISCUSSION**，orchestrator 代筆、3 項裁決已回填 | `design/D-013-conversation-scope-pk.md`（64 行） | **重派 architect-reviewer + design-reviewer 走 R2 雙審**，兩份任務單的重點見 §4 |
| T-022 D-013 實作 | **阻塞**（等 D-013 APPROVED） | – | D-013 通過雙審 + 使用者核可後才動工 |

## 2. 尚未落入文件的口頭決策
**無。** 本輪 6 項使用者裁決全部已寫入文件：
1. 日期炸彈修在獨立分支先併 main → 已完成（PR #11）。
2. T-018/019/020 走 subagent 完整審查 → 已完成。
3. T-020 取消側聚合「改實作、不改設計」→ 已補 `D-012` §一.3 errata + 討論紀錄 #4。
4. (N2) 靜默死角「這輪就修掉」→ 已修，`D-004` errata 第 6 條。
5. PK 變更「先做完再一起 merge」→ `D-013` + task-board 阻塞清單。
6. D-013 上線順序「單階段」、NULL 列「DELETE」→ `D-013` 討論紀錄 #1/#2。

## 3. 已知技術債與風險
- **本輪新增 5 項已入 task-board Backlog**，最該記住兩項：
  1. **`signup` 的 `available` 仍用交易外快照 `event.capacity`**——今日安全**僅因**D-010 G1 保證 capacity 單調不減。**若日後實作「縮減名額」，該行必須改為鎖內 `fresh.capacity`，否則靜默超賣。**
  2. **測試檔不受 `tsc` 型別檢查**（`tsconfig` 排除 `*.test.ts`、eslint 未開 type-checked）⇒ 介面新增必填欄位時漏改測試呼叫端會以 `undefined` 靜默通過＝**假綠**。本輪 `AbortInput.groupId`/`NextRoundInput.groupId` 屬此風險，已 grep 核對無漏。**這是本專案第 3 次假綠類問題。**
- **`harness/LESSONS.md` 本輪首次實際回寫三則**（此機制自建立以來從未運轉過，「已回寫紀錄」表原為空）。T-017 的清償欠款仍未處理。
- `docs/task-board.md` 現 78/80——再加條目前須先歸檔（刻意的壓力機制，勿繞過）。
- **D-013 的 migration 0004 尚有一個未驗證的假設**（見 §4）。

## 4. 阻塞與等待事項
- **D-013 R2 雙審未完成**（兩位 reviewer 皆 529 失敗）。重派時務必保留兩個重點：
  1. **給 architect-reviewer**：`design/D-013` §一.1 第 3 步寫 `DROP CONSTRAINT conversation_states_pkey`，但 `0001_init.sql:76` 用的是 `line_user_id TEXT PRIMARY KEY` 隱式命名——**該約束名稱是「推斷」而非查證**。若推斷錯，migration 會在 PROD 直接失敗。**最快的驗證是實測而非審查意見**：`docker compose up -d` 後跑
     `docker exec golf-reserv-pg-test psql -U golf -d golf_test -c "SELECT conname, pg_get_constraintdef(oid) FROM pg_constraint WHERE conrelid = 'conversation_states'::regclass;"`
     （本 session 已準備好此指令但未執行）。另請它評估 Neon pooled 下 `ALTER TABLE` 被長交易卡住的風險，供 runbook 落實。
  2. **給 design-reviewer**：一個未解的 UX 張力——PK 改複合鍵後跨群流程並行，使用者在 A、B 兩群各有一段開團流程，而 bot 的提問文案（「請輸入活動日期」）**兩群一模一樣、不含群組資訊**，可能搞混自己在回答哪一群；但加入區辨資訊又可能撞到「不洩漏其他群」原則。**本 session 沒有答案。** 另請它判斷「已結束你先前未完成的分組。」對不知道兩者共用 slot 的使用者是否莫名其妙。
- **T-021 的 (N2) design 複審未完成**。送審時要點名的核心疑慮：`abandoned:'create'` 那句只在別群可達，**看到它是否等於確認「前一段流程在別群」**？若成立即為 blocker（自己犯了要修的那類洩漏）。**注意：D-013 實作後 `'create'` 分支會被移除為死碼（G4），此疑慮將自動消解**——因此若 D-013 即將動工，此複審可併入 T-022 那輪，不必單獨跑。
- **merge 條件**（使用者裁決）：T-022 完成 → 一次把 `feat/D-011-grouping` 併入 `main`。**merge 後的部署不是純 code deploy，會多一道 migration 0004**（單階段：先 migrate 再立即部署新 revision；該窗口內開團/分組指令可能失敗、重打即可）。runbook 需補這段。

## 5. 給接手者的第一句話
先確認 API 是否恢復；恢復了就從 **§4 第 1 點的 psql 實測**開始——那是 D-013 唯一一個「錯了就會在 PROD 炸」的假設，且一條指令就能查證，比任何審查意見都可靠。接著重派 D-013 的 R2 雙審（兩份任務單要帶上 §4 的兩個重點，並明告 reviewer「本設計為 orchestrator 代筆、未經專職 architect 把關，請嚴格審」），通過並經使用者 APPROVED 後才動工 T-022。

分支上四個任務的成果都已驗證且 commit，**不要重做**；唯一還欠的關卡是 T-021 的 (N2) design 複審，而它很可能會被 D-013 的實作直接消解。
