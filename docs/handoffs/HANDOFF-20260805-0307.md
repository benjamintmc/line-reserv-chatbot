# HANDOFF — 交接快照

> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**
> 本輪 session 的最終快照，取代同日 `-0200` 與 `-0252` 兩份（那兩份寫於 PR 尚未合併時，
> 其中的「有 PR 開著」警示已不適用）。**接手請只讀本份。**

- 日期 / 交接原因：2026-08-05 03:07 ／ 本輪工作全數完成並合併，session 收尾
- 目前 Phase：PROD LIVE 維運中。本輪全部是**框架與文件工作，未動任何產品程式碼**
- harness 版本：**1.4.0**（見 `harness/VERSION`，含四項在地化偏離）

## 0. 起手狀態

- **`main` 已包含本輪全部成果，沒有任何 PR 開著、沒有未合併分支。**
- 工作區乾淨；`npm run harness:check -- --strict` 全綠；274 tests 全綠。
- CI（GitHub Actions）每次 push 自動跑：lint → build → test（含 Postgres service）→ 框架關卡 → commit 格式檢查。

## 1. 本輪做了什麼（PR #3–#9）

| PR | 內容 |
|---|---|
| #3 | harness 1.1.0 → **1.4.0**：補齊 1.2/1.3/1.4 三代缺件、CLAUDE.md 回填、反向文件化 `01-architecture` 與 `02-api-contract`（原為空白模板）、修 3 個 checks 缺陷、建立 CI |
| #4 | 清掉 7 條已解決／已過時的 Backlog 假待辦 |
| #5 | 「我的球聚」規格定案（5 項裁決 + 功能意圖） |
| #6 | H1／H2 列入 Backlog + P1 push 費用研究 + LINE 平台限制移入 `01-architecture` |
| #7 | 交接快照（已被本份取代） |
| #8 | README 改寫為中性約球定位 + 校正嚴重過時內容 |
| #9 | 完成球種中性化（CLAUDE.md §0、project-brief、requirements 加註） |

**三個 checks 缺陷值得記住**：cp950 主控台導致的**假紅**、`check_doc_budget.py` 在 Windows 因路徑分隔符而完全失效的**假綠**、`check_ac_coverage.py` 認不出粗體 `**APPROVED**` 的**假綠**。後兩者代表關卡曾經宣稱通過但根本沒跑。

## 2. 進行中任務的中間狀態

| 任務 | 狀態 | 下一步的具體第一個動作 |
|---|---|---|
| T-016 harness 升級 | **DONE** | 無 |
| T-017 LESSONS 回寫清償 | **BACKLOG** | 讀 `harness/LESSONS.md` 問題登記表，把 5 項達門檻項目各寫成「建議措辭 + 落點」，交使用者逐條核可 |
| D-009 我的球聚 | **規格定案，未動工** | 派 architect 寫 `design/D-009`。**規格已全定，無待裁決事項** |
| D-010 H1 開團後加開名額 | **已登記，未設計** | 需 `design/D-010`（R2）。可複用 T-015 的 `promotionQuota` 鎖內重算路徑 |
| H2 關閉報名時 @正取者 | **已登記，未設計** | R1。**動工前需先裁決**：正取人數超過 mention 上限時的降級行為 |
| P1 開球前提醒 | **僅完成前期研究，未決定是否實作** | 先實測「非好友能否被個別推播」（見 §4） |

## 3. 尚未落入文件的口頭決策

**無。** 本輪所有裁決都已寫入 CLAUDE.md、task-board 或 01-architecture。以下三項因**容易被接手者誤讀為缺口**，特別在此重申：

1. **「我的球聚」不列候補，是本意不是遺漏。** 使用者定調的功能意圖是「列出所有**已經確定的**球敘，讓使用者知道自己該去哪幾場即可」——候補不算確定。**請勿順手補上。** 候補者的能見度（原 P3）已裁決不列入 Backlog。
2. **球種中性是決議（D-005 §3），已寫入 CLAUDE.md §0 成為憲法條文。** 新增任何 user-facing 文案不得引入特定球種用語。`design/D-004`、`D-005`、`requirements.md` 中殘留的「高爾夫」字樣**刻意保留**——分別是測試資料範例、決議本身的紀錄、以及歷史文件，改動等同竄改決策史。
3. **H3（複製上一場）／P2（取消安全網）／P3（候補能見度）經裁決不列入 Backlog**，本快照僅為紀錄，避免日後重複提案。

## 4. 已知技術債與風險

- **產品面**：6 項，索引在 `docs/01-architecture.md`「【技術債】現況清單」。最該優先的是**拒絕回覆的去重政策不對稱**（同型問題已 3 次，會讓使用者看到重覆回覆；現況已寫入 `02-api-contract.md` 作為回寫基準）。
- **`docs/task-board.md` 已達 79/80 行**。再加 Backlog 條目會被 `--strict` 擋下，屆時需再歸檔或壓縮。**這是刻意的壓力機制，不是故障——請勿繞過。**
- **`CLAUDE.md` 已達 150/150 行**。同上；且 §0–§9 **編號已凍結**（多份 APPROVED 文件硬引用 §4、§5），新增章節只能附加於 §7 後或以 §x.5 插入。
- **`check_doc_budget` 對導入日前既有的 8 份 D 文件豁免**（`harness/doc-budget-exempt.txt`）。**新增的設計文件不得列入該檔**，120 行上限對它們仍有效。
- **P1 若實作會打破一個現有架構前提**：系統至今只用 `replyMessage`（不計費）。主動提醒必須用 `push`/`multicast`，計費且**推播到群組是按群組總人數計**（30 人群組推一則＝30 則）。台灣輕用量 200 則/月且**不可加購，超出即 API 錯誤且訊息不送出**（會靜默失效）。試算與方案表見 `01-architecture.md`「訊息費用結構」。

## 5. 阻塞與等待事項

- **無阻塞。**
- **待實測（P1 前置）**：個別推播是否要求對方已將官方帳號加為好友。若非好友推不到，P1 的價值會大幅縮水——**建議用真帳號實測後再談設計**。
- **待裁決（H2 前置）**：正取人數超過 mention 數量／訊息長度上限時的降級行為（截斷並附「等 N 人」或改列名不 mention）。

## 6. 給接手者的第一句話

`main` 就是最新狀態，直接開工即可——本輪沒有留下任何未合併的東西。

三條路，依價值排序：**T-017 LESSONS 回寫清償**（這套 harness 唯一從未運轉過的機制；欠款裡「拒絕回覆去重」和「errata 治理」各已出現 3 次，是真的在反覆咬人）、**D-009 我的球聚**（規格已定，可直接派 architect）、**D-010 加開名額**（使用者痛點最尖銳——現況沒有任何編輯指令，唯一 workaround 是取消重開，會讓報名全部歸零、候補 FIFO 全毀）。

最後一句提醒：本輪修好的三個 checks 中有兩個原本是**假綠**。`npm run harness:check` 的綠燈現在才可信——但也正因如此，**不要只看它有沒有通過，偶爾要驗證它是否真的會失敗**（LESSONS 2026-08-05 已登記此教訓，候選對策是給 check 加 self-test）。
