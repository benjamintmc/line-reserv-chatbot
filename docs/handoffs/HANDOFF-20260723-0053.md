# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-07-23 00:53 / 使用者收工（「先 COMMIT，剩下的明天繼續」）
- 目前 Phase：M1（資料層與指令解析）— 資料層 T-004 已 DONE；下一個 mini-cycle 是 D-002 報名核心設計（Phase 3 設計）
- harness 版本：1.1.0

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-005 | 指令解析（+N/-N/名單/開團）；狀態 READY，尚未動工。需先開 D-002 設計 | 尚無程式碼；欄位可對齊 `src/db/`（D-001 已定案） | 派 backend-engineer 撰寫 `design/D-002-*.md`（報名核心：+N/-N、額滿判斷、整批轉候補、FIFO 遞補、名單訊息組版），走三段式；完成後 Orchestrator 與使用者討論 → APPROVED |
| — | M1 資料層 T-004 已完整交付並 DONE（非進行中，供接手者背景） | `src/db/`（index/migrate/schema/time/migrations/0001_init.sql/repositories×6）、`design/D-001-data-model.md`(APPROVED)、`docs/adr/ADR-001~003` | 無（已 DONE，可直接被 D-002 消費其 repository 原語） |

## 2. 尚未落入文件的口頭決策
（本 session 的所有裁決均已落入文件，無遺漏）
- Q1/Q2/Q3（is_host 保留不寫 / 日期存文字 / 取消採 soft-delete）→ 已入 `design/D-001-data-model.md` 討論紀錄。
- CLAUDE.md §4 加註 better-sqlite3 版本以 ADR-003 為準 → 已入 CLAUDE.md（經使用者同意）。
- fastify 5 / vitest 4 升級決策 → 已入 commit bc5e386 與 task-board Backlog。

## 3. 已知技術債與風險
- **（M5 部署前必處理）** `build: tsc` 不複製 `src/db/migrations/*.sql` 到 `dist/`，生產跑編譯版 migrate 會失敗。開發路徑（tsx/vitest）不受影響。處置擇一：postbuild copy、生產以 tsx 跑 migrate、或 .sql 內嵌字串。詳見 task-board Backlog。
- **（D-002 落實）** 報名/取消/遞補交易一律經 `runImmediate` 封裝（G2 守門對 DEFERRED 交易為盡力非強制）。`DATABASE_PATH` vs `DATABASE_URL` 於切 PG 時於 config 併容。
- 環境：本機 `python`/`python3` 是 Windows Store stub（exit 49、無輸出），跑 harness Python 檢查一律用 `py`（例：`PYTHONIOENCODING=utf-8 py harness/checks/check_ac_coverage.py`）。better-sqlite3 pin `^12.4.1`（Node 24 ABI 137 prebuilt，見 ADR-003）。
- AC 覆蓋檢查標記格式須為 `[D-001 AC-n]`（含 D-xxx 前綴），否則掃不到。以上三項已登記 harness/LESSONS.md（首次出現，觀察中）。

## 4. 阻塞與等待事項
- 無阻塞。Node/npm 已就緒，資料層全綠（build 零錯、40 tests、AC 13/13、npm audit 0 漏洞）。

## 5. 給接手者的第一句話
現在最重要的一件事：以 Orchestrator 身分向使用者報到，然後派 backend-engineer 撰寫 **D-002 報名核心設計文件**（+N/-N、額滿整批轉候補、FIFO 遞補、名單訊息組版），這是 M1 後半的起點；D-002 可直接建立在已 DONE 的 `src/db/` repository 原語之上（`runImmediate`/`countConfirmed`/`insertSlots`/`cancelByIds`/`pickWaitlistForPromotion`/`promoteByIds`）。切記：設計未 APPROVED 不得動工實作。
