# HANDOFF — 交接快照

> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-08-05 ／ harness 1.4.0 升級（T-016）完成，session 收尾
- 目前 Phase：PROD LIVE 維運中；本次為框架維運任務，**未動任何產品程式碼**
- harness 版本：**1.4.0**（自 1.1.0 直升，見 `harness/VERSION` 含三項在地化偏離）

## 1. 進行中任務的中間狀態

| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| T-016 | **DONE**。五階段全完成並各自成一個 commit | `harness/`、`.claude/agents/`、`CLAUDE.md`、`docs/01-architecture.md`、`docs/02-api-contract.md`、`docs/api/openapi.yaml`、`.github/workflows/ci.yml`、`scripts/harness-check.mjs` | 推分支 `chore/harness-1.4.0` 並確認 GitHub Actions 首跑全綠 |
| T-017 | **BACKLOG**（本輪依使用者決策不做） | – | 讀 `harness/LESSONS.md` 問題登記表，把 5 項達門檻項目各寫成一則「建議措辭 + 落點」交使用者逐條核可 |

## 2. 尚未落入文件的口頭決策

- 使用者於本次選定：①02-api-contract **重定位為指令契約**（非 REST）②checks 缺陷修復**併同** GitHub Actions
  ③LESSONS 回寫**本輪不做**、另開任務。前兩項已落入程式碼與文件，第三項已落為 task-board T-017。
- 我方判斷（已落入 `docs/worklists/README.md` 與 `harness/VERSION`）：worklists 只建 4 份實際使用的角色，
  其餘隨用隨建——理由是本專案 15 個任務裡實質只有 backend-engineer 一個實作角色。

## 3. 已知技術債與風險

- **產品面**：6 項，已在 `docs/01-architecture.md`「【技術債】現況清單」建立索引並交叉指向 task-board Backlog。
  其中最該優先處理的是**拒絕回覆的去重政策不對稱**（同型問題已出現 3 次，會造成使用者看到重覆回覆）。
- **框架面**：`check_doc_budget` 對既有 8 份 D 文件豁免（`harness/doc-budget-exempt.txt`）；
  **新增的設計文件不得列入該檔**，120 行上限對它們仍有效。
- **CI 尚未實跑過**：workflow 已寫好但分支還沒推上去，PG service 與 `--strict` 兩處是首跑最可能出問題的地方。

## 4. 阻塞與等待事項

- 無阻塞。分支 `chore/harness-1.4.0` 有 5 個 commit，尚未 push、尚未開 PR。

## 5. 給接手者的第一句話

框架已經升到 1.4.0 且文件斷層補完了，**現在最重要的一件事是把 `chore/harness-1.4.0` 推上去確認 CI 真的會跑**；
之後接 T-017——那是這套 harness 唯一從未實際運轉過的機制（回寫），而 LESSONS 裡已經堆了 5 項達門檻的欠款，
其中「拒絕回覆去重政策」和「errata 治理」各出現 3 次，是真的在反覆咬人的問題。
