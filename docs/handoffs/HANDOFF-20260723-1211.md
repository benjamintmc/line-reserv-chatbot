# HANDOFF — 交接快照（session 結束 / 換模型前由 Orchestrator 填寫）

> 填寫完存為 `docs/handoffs/HANDOFF-{{YYYYMMDD-HHmm}}.md`。
> 原則：**只存在於對話中而不在此文件裡的資訊，視為不存在。**

- 日期 / 交接原因：2026-07-23 12:11 / 使用者收工（「先 commit 晚點再繼續」）
- 目前 Phase：M2（報名核心）Phase 3 設計尾聲 — D-003 已審查通過、**待使用者最終 APPROVED**
- harness 版本：1.1.0

## 1. 進行中任務的中間狀態
| 任務 ID | 進度描述 | 已完成部分的路徑 | 下一步的具體第一個動作 |
|---|---|---|---|
| D-003 設計 | DRAFT，已過 architect-reviewer + nit errata（getGroupMemberProfile、防禦性 assert）。OP-1~4 已裁決。**只差使用者最終 APPROVED** | `design/D-003-registration-core.md`（狀態仍 DRAFT） | **下次 session 第一件事：向使用者確認是否標 D-003 APPROVED**。APPROVED 後：把文件狀態改 APPROVED、task-board D-003 改 APPROVED、T-006 由 BLOCKED→IN_PROGRESS，派 backend 實作 |
| T-006 實作 | BLOCKED（等 D-003 APPROVED） | 尚無程式碼 | 實作順序：①先在 D-001 資料層補新原語 `RegistrationRepository.findActiveProxyByName(eventId, displayName): RegistrationRow[]`（跨 owner、cancelled_at IS NULL、依 seq）②建 `src/domain/`（registration-service / roster / list-formatter）③改寫 `src/webhook/handler.ts`（buildReplies→parseCommand 分派、async profile fetch、注入 repositories/lineClient）④單元測試覆蓋 AC-1~AC-19 且標記 `[D-003 AC-n]` |

## 2. 尚未落入文件的口頭決策
（本 session 所有裁決均已落入文件）
- D-003 的 OP-1~OP-4 裁決（R1 / 主辦 override 納 M2 / 顯示候補 / 僅 open）→ 已入 `design/D-003-registration-core.md` 討論紀錄。
- **待辦**：D-003 最終 APPROVED 尚未取得（使用者選擇收工），非「已確認未落檔」，而是「尚未確認」——下次需重新徵得。

## 3. 已知技術債與風險（task-board Backlog 有完整清單）
- **T-006 實作重點**（architect-reviewer D-003 審查提醒）：
  - 群組取顯示名務必用 `getGroupMemberProfile(groupId, userId)`（非 getProfile，後者對未加好友者 404）——已寫入 D-003 §0/§2/§7 + AC-19。
  - handler 需轉 async + 注入 repositories/lineClient；server.ts 事件 loop 相應改動（nit-3）。
  - e2e 至少涵蓋 AC-17（主辦跨 owner 代取消觸發 FIFO 遞補）（nit-1；D-003 含授權+刪除類，字面近 R2，惟已走雙審密度、R1 維持）。
- **M5 部署前必處理**：`build: tsc` 不複製 `src/db/migrations/*.sql` 到 dist，生產 migrate 會失敗。
- **環境**：本機 `python`/`python3` 是 Windows Store stub（exit 49），harness Python 檢查一律用 `py`（`PYTHONIOENCODING=utf-8 py harness/checks/check_ac_coverage.py`）。better-sqlite3 pin `^12.4.1`（ADR-003）。tsconfig 已排除測試檔出 dist、vitest 只掃 `src/**/*.test.ts`。

## 4. 阻塞與等待事項
- **D-003 待使用者最終 APPROVED**（唯一阻塞 T-006 的關卡；設計與審查皆已就緒）。
- 無環境/技術阻塞。目前 build/測試全綠（M1 為止 83 tests、AC 39/39）。

## 5. 給接手者的第一句話
現在最重要的一件事：以 Orchestrator 身分向使用者報到，摘要「M0/M1 已完成並推上 GitHub；D-003 報名核心設計已通過 architect-reviewer 審查、只差你點頭」，然後**請使用者對 D-003 做最終 APPROVED**。一旦 APPROVED，即派 backend 實作 T-006（先補資料層 `findActiveProxyByName`，再 domain/handler，覆蓋 AC-1~AC-19）。切記：設計未 APPROVED 不得動工實作；D-003 目前仍是 DRAFT。
